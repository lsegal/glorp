name: CI
on:
  push:
jobs:
  test:
    if: github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip ci]')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 10.34.4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
      - uses: actions/setup-node@v4
        with:
          node-version: "24"
          cache: pnpm
          cache-dependency-path: web/pnpm-lock.yaml
      - run: pnpm install --frozen-lockfile
        working-directory: web
      - run: pnpm run build
        working-directory: web
      - run: pnpm run lint
        working-directory: web
      - run: pnpm test
        working-directory: web
      - run: go test ./...
      - run: go vet ./...
      # macOS has no runner here, so cross-compile its process handling to keep
      # it from breaking unnoticed. Windows has its own job below.
      - run: GOOS=darwin go vet ./...

  test-windows:
    if: github.event_name != 'push' || !contains(github.event.head_commit.message, '[skip ci]')
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version: "1.23"
      # The default build needs no generated web assets: the web/dist embed is
      # behind the production tag, so Go alone covers the Windows-only process
      # handling in orphanguard_windows.go and process_windows.go.
      - run: go test ./...
      - run: go vet ./...
