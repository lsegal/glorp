# Changelog

## Unreleased

- Fill the web UI settings modal's Models tab with the models each agent's CLI actually reports, instead of listing bare agent names with no model behind them (issue #589). The tab built its chips only from an agent definition's static model allow-list, and no built-in definition declares one -- the models are discovered by the same probe `glorp agents` prints -- so every chip fell back to the agent's bare name (`claude`, `codex`, `gemini`, ...), which is a spec that names no model at all and so cannot express the choice the multiselect exists to make. The tab now feeds the model lists from `GET /api/agents` (the endpoint whose auth and quota readings the chips and the status lines already show) into the chip list, so each chip is a real `agent/model` pair as soon as the probe answers, and an agent no model list could be built for contributes no chip rather than a nonsensical one -- while a spec the run is already dispatching with stays selectable so an active choice can always be switched back off. The tab also says `probing agents for models...` while that probe is still running rather than showing a misleading list, an agent the probe never answered for reads `unavailable` on its status line instead of sitting on `probing...` forever, and an agent it listed no models for says so on the same line, which is where the missing chips are explained.

## v1.3.1 - 2026-09-04

- Show each agent's auth and quota reading as always-visible text on the web UI settings modal's Models tab, not only as a chip's hover-only tooltip (issue #585). Moving the multiselect from the Agents tab onto the Models tab (issue #582, below) kept the reading available, but only inside a `title` attribute you had to hover a chip to see, so it read as if the status had disappeared entirely. The tab now also lists one line per agent underneath the chips with the same auth/quota/installed text the old Agents tab rows showed, while the chips themselves keep driving the model multiselect.
- Prioritize running and errored jobs in the "last 6" dashboard viewports instead of showing whichever jobs happened to start most recently (issue #587). Both the terminal dashboard and the browser dashboard cap the job grid at 6 cards and, until now, picked those 6 purely by start time, so a long-running job could scroll out of view behind newly queued or already-completed ones. Jobs are now ordered running first, then errored, then any other in-flight job, with completed jobs pushed to the bottom; start time (most recent first) still breaks ties within each group.
- Fix the web UI's settings modal hanging on "loading settings..." indefinitely when a request lands before `glorp`'s run loop is ready to service it. `ApplySettings` and the job retry/stop handler hand a request to the run loop over an unbuffered channel and block until the loop's dispatch `select` receives it, but the web server starts accepting connections well before that: loading work state, `resetFailedWork`'s GitHub calls, and in webhook mode ngrok tunnel startup and per-target webhook configuration in between can all take a real amount of time, and nothing bounded the wait. Both handlers now fail with a `503` after a short bound if the run loop still hasn't reached readiness, and the dashboard retries the settings fetch with backoff on that response instead of getting stuck showing the spinner forever.
- Move the web UI settings modal's multiselect from an Agents tab onto a Models tab, and render it as selectable chips instead of checkboxes (issue #582). The Agents tab let you toggle whole agents, which cannot express choosing a model -- you can't pick `codex/gpt-5.6` by checking `codex` -- so the tab is now Models, and each chip is one `agent/model` pair (or the bare agent name for one with no declared model list); selecting a chip drives which agent dispatches the same way the old checkboxes did, and each chip's tooltip still shows its agent's auth and quota reading.
- Add an Agents tab to the web UI's settings modal, so the agents a run dispatches to can be toggled with a multiselect, and each one's install, sign-in, and quota state is visible without leaving the browser (issue #572). The settings modal previously took one free-text `--agent`-style spec with a datalist of suggestions, which could name only a single agent at a time even though `glorp watch --agent` itself accepts several for round-robin dispatch; the live override now takes a set (`POST /api/settings {"activeAgents": [...]}` ), and the modal's new Agents tab renders one checkbox per registered agent instead. Each row also reads a new `GET /api/agents` endpoint, which reuses the same probe `glorp agents` prints to the terminal -- install path and version, sign-in state, quota, and models -- so the browser dashboard shows the CLI's actual state rather than only the names `--agent` would accept.
- React with 👀 to a comment that directly mentions a glorp instance with `@/glorp:ID`, so a human watching the thread can see the mention was read even before (or regardless of whether) it goes on to trigger a run. The reaction is posted as soon as the webhook delivery is decoded, ahead of the existing check that the mention is still the last comment and from an allowed commenter, since a stale or unauthorized mention was still seen. Reacting goes through the same `gh api` comment client that already posts and lists comments; a comment source with no reaction affordance (there is none yet) simply skips it.
- Ship a built-in definition for [Antigravity CLI](https://antigravity.google/docs/cli/overview) (`agy`), so `--agent agy` dispatches to it alongside `codex`, `claude`, `gemini`, `muse`, `opencode`, and `cline`. It runs non-interactively with `agy --print`, accepts `--model` and `--effort low|medium|high`, and bypasses its permission prompts with `--dangerously-skip-permissions` under `--yolo`. `agy` has no documented way for a headless caller to capture or assign a resumable conversation id (see [google-antigravity/antigravity-cli#7](https://github.com/google-antigravity/antigravity-cli/issues/7)), so the definition declares `"session": {"assign": "none"}` like `opencode` and `cline`: a recovery restarts the work with the recovery prompt rather than resuming a session, and `gh-fix`'s re-entrancy picks the branch and draft pull request back up regardless.
- Fix the web UI's settings modal intermittently failing to load with "settings unavailable". `glorp`'s web dashboard started serving HTTP requests before wiring up the handlers that back `/api/settings` and the job retry/stop buttons, so a request that landed in that startup window saw the handler as unset and got a `503`, which the settings modal had no retry for and so was left stuck showing the error. The server now only starts accepting connections once its handlers are wired, so no request can land in that gap.
- Show both the terminal dashboard and the browser dashboard by default instead of picking one. `glorp watch` used to select exactly one UI through `--ui web|tui|none` (with `--no-ui` as an alias for `--ui none`), so watching from a terminal meant choosing between the two. `--ui` and `--no-ui` are replaced with independent `--no-tui` and `--no-webui` flags: the terminal dashboard is shown whenever stdout is a terminal, the browser dashboard is shown alongside it, and either can be turned off on its own.
- Enumerate the models each agent takes in `glorp agents` by asking its CLI, instead of answering `models    any model the CLI accepts` for five of the six built-ins. Only `opencode` shipped a listing probe, because it was the only one of the six whose CLI has an obvious command to ask; the other five fell through to a note that is true and useless, since the reason to read that field is to find a name to write after the `/` in `--agent`. Every built-in now reads its list off the CLI itself rather than out of a list written into glorp, which is stale the morning after a vendor ships a model: `codex` prints its catalog with `codex debug models`, `cline` and `gemini` have no listing subcommand but answer over their agent-client protocol, so the probe runs `--acp` and reads the models the new session reports, and `muse` answers `model/list` over `muse serve`. Two definition fields make that possible: `doctor.modelsJSON` names where the ids are in a JSON answer, as a dotted path whose `[]` walks an array and whose `[key=value]` keeps only the elements a field marks, so a catalog's hidden rows stay out of the report; and `doctor.modelsStdin` is the handshake the probe writes to a CLI that only answers over a stdio protocol, sent with the pipe held open, read until the reply carries models, and then shut down, so a command that serves rather than exits is asked without hanging the report. `claude` is the one built-in that cannot be asked -- its CLI has no listing command and no protocol that carries one -- so it declares `doctor.modelsNote`, which may now stand on its own, and the report says the CLI does not list its models rather than naming models glorp cannot check. An agent whose CLI is signed out of its provider answers the handshake with an empty catalog, and the report names the command that could not list it instead of falling back to `any model the CLI accepts`. `doctor.knownModels` survives for a config file describing a CLI glorp has never seen, but no built-in declares one any more, and a test holds them to it so the next definition cannot ship with a hardcoded list.
- Read the Claude quota out of the CLI's output tolerantly, so a machine whose `claude` prints anything beside its JSON result still reports a quota. The reader decoded the whole of stdout as one JSON object, which is what `claude --print --output-format=json` is documented to produce and not what it always produces: a diagnostic line from something it loaded on the way -- an MCP server that advertises no tools, for one -- lands on the same stream, the strict decode fails on the first character after the object, and `glorp agents` reported `quota unavailable: invalid character 'C' after top-level value` with `auth unknown` under it, because sign-in for Claude is inferred from the quota reading and there was none. The reader now decodes the JSON values on stdout one at a time and takes the first non-empty `result`, and falls back to the raw output when none of it decodes, which still works because the `Current session: N% used` lines it matches survive JSON encoding unchanged. A signed-in account reads `session N% left, week N% left` again in both the watch status bar and `glorp agents`, and the report's sign-in line follows it back to `signed in`.
- Fix the Codex quota reader, which had been running `codex app-server --stdio` against a CLI that no longer takes that flag. Codex rejected the argument before reading a byte of the JSON-RPC handshake glorp wrote into it, so every read failed and Codex reported no quota at all: nothing beside `codex` in the watch status bar, and a bare `quota unavailable` in `glorp agents`. `app-server` speaks JSON-RPC over stdio by default, so the reader now names no transport at all and a signed-in account reads its quota again. The reason a quota reader could not answer is no longer thrown away either: `glorp agents` prints it after `unavailable`, so a reader broken by a CLI that changed its flags says which argument it choked on instead of looking the same as an account that is merely signed out.
- Turn `glorp agents` into a report on the agents it lists, so the models an agent accepts are something you can read rather than something you have to guess. The command printed one agent name per line and nothing else, which said nothing about whether the CLI behind a name was installed, whether it was signed in, or -- the question that actually blocks a run -- what to write after the `/` in `--agent`. No built-in definition enumerates its models, and for a provider-agnostic CLI none usefully could: `opencode` fronts whatever its configuration gives it, twenty models on one machine and a different twenty on the next. `glorp agents` now prints a block per agent with the resolved binary and its version, the sign-in state, the quota, and every model written as the fully qualified `agent/model` name, one per line, ready to paste into `--agent`. An agent whose CLI is missing is marked and probed no further, and one whose version is below its definition's `minVersion` is marked with what it needs, so the report doubles as the check a failing dispatch sends you looking for. Two of those answers cannot come from a definition glorp already has, so definitions may now declare them: a `doctor` block names an optional `auth` argv whose exit status (narrowed by an optional `signedIn` expression, for the CLIs that report a signed-out account on a zero exit) reports sign-in, and an optional `models` argv whose stdout lists the models one per line (filtered by an optional `modelPattern` for a decorated list), both substituting `{binary}` so `--agent-binary` reaches them, and both bounded by a `timeout` because a report is a diagnostic and is never allowed to hang. `codex` and `opencode` ship probes -- `codex login status` and `opencode auth list`/`opencode models` -- and the other four deliberately do not, because their only sign-in check is a device-code flow, and a diagnostic command that logs somebody in for asking is worse than one that says `unknown`. Sign-in falls back to the quota reading where there is no probe, since every quota reader asks the CLI something only a signed-in account can answer, which covers `claude` for free; an agent with neither is reported as `unknown` rather than guessed at, and one that reports no quota by design is now told apart from one whose reader could not answer. Agents are probed concurrently, so the report costs the slowest CLI rather than the sum of them. The plain listings both installers and any script parse are unchanged and run no probe at all: `-skills` prints the same target ids it always did, and the previous default output is `-names`.

## v1.3.0 - 2026-09-04

- Read glorp's `GLORP_CHECKOUT_DIRECTORY=` marker off an agent's raw stream as well as its decoded text, so a run announcing its isolated checkout through a shell tool reports the directory instead of leaving the dashboard on `checkout: pending` for the whole job. The marker was matched only against decoded output lines, which is where `claude` and `codex` put it because they narrate the path as assistant text; `cline` runs the announcement through a command instead, so the marker arrived inside a `content_end` tool result -- a field the JSONL decoder never reads, because a definition maps text and tool names and nothing else -- and a completed cline job's record kept an empty checkout directory even after it merged its pull request. `opencode` maps its stream the same way and had the same gap. The marker is glorp's own protocol rather than any agent's, so it is now scanned before decoding rather than by asking each definition to name another field path: a raw line that parses as JSON has every string in it walked, whichever key or nesting the agent chose, and each line inside a tool's output is scanned on its own; a line that is not JSON is scanned as it is written, which is what a plain-output agent produces. The decoded scan stays, because an agent whose text events stream token-sized fragments -- `muse` -- can split the marker across raw lines that only reassemble once decoded, and both scans share one capture so the checkout is still reported exactly once. The existing guards are unchanged, so the marker's own documentation read back by an agent, a relative path, or a directory that does not exist still records nothing.
- Let a JSONL agent definition name field paths that apply only to a given event type, so a CLI whose envelope spreads one logical event over several typed events is readable at all. The generic decoder read one `text`/`toolName`/`toolInput` path set for the whole stream, which describes an envelope whose events share a field layout and cannot describe one whose events do not: a path pointed at where one type keeps its tool name reads something else, or nothing, on every other type. The `output.jsonl` block now takes an `events` object keyed by the value `type` names, whose entries override `text`, `toolName`, `toolInput`, and the new `toolNamePrefix` for that type alone. An override names only what differs; `text` is overridden on its own, while `toolName` carries `toolInput` and `toolNamePrefix` with it, since pairing one type's name with another type's input path renders another call's arguments. `toolNamePrefix` is the second half of the same problem: a stream whose name field doubles as a kind covering more than tool calls is read by naming the prefix that tells them apart, and only a name carrying it is rendered, with the prefix trimmed off. Validation rejects `events` without `type`, an empty event type, an override that is also in `ignore` (nothing is ever read out of it), an override that reads neither text nor a tool name, a malformed path in one, and either `toolInput` or `toolNamePrefix` without the `toolName` it belongs to; the shared paths may now be left empty entirely when the overrides read something, and the block is still rejected on any format other than `jsonl`. A definition that names no overrides decodes byte-identically to before, which is what `cline` and `opencode` are pinned to. `muse` uses it: `task.lifecycle.proposed` carries the tool's name at `payload.task.kind` as a task kind shared with model turns -- `tool.read_file` beside `model.meta.response` -- so the definition reads the name off that event type alone and matches the `tool.` prefix, and a Muse run now shows `Running: read_file` as it works instead of text alone. The name renders without a detail after it because no Muse event carries the input the call was made with: `tool.result` pairs the name with the tool's entire output rather than a summary of the call, and arrives after the call has already run.
- Let a JSONL agent definition declare that its text events carry a fragment of a message rather than a whole one, so a CLI that streams token by token shows the sentences it writes instead of a line per token. The generic decoder ends a line on every event, which is right for an envelope whose text events carry whole messages -- `cline`'s `content_end`, `opencode`'s `text` -- and wrong for one that streams deltas, and it is the reason Muse was the one surveyed agent left on plain output. The `output.jsonl` block now takes `"textDelta": true` beside `"text"`: the decoder buffers what `text` names and writes it as one line on the first event that carries no text -- a tool call, an event of a shape the definition describes nothing in, a line that is not JSON at all, or the end of the stream -- joining the fragments with the spacing they were sent with rather than trimming each one into a word. An event type in `ignore` is still dropped before anything is read out of it, so a bookkeeping event repeated mid-message does not split the sentence in two, and a banner or a stack trace ends the message first rather than landing in the middle of it. The field needs `text`, is rejected on any format other than `jsonl` along with the rest of the block, and a definition that leaves it out decodes byte-identically to before, which is what `cline` and `opencode` are pinned to. `muse` switches to it: `muse exec --json` on a fresh run and a resume, reading the event type from `payload_type` and the text from `payload.text` with `textDelta` set, ignoring `tool.result` because its `payload.text` is the tool's entire output rather than a summary of the call, and ignoring the three `run.terminal.*` events because they repeat the whole message once the run is over -- which is the text plain mode already printed, and exactly the "nothing until the agent finishes" this was meant to fix. Its contract test now pins the decoded output against a captured stream. The one-shot vision read stays on plain output, whose answer is parsed as prose, as it does for Claude and opencode. Muse progress lines follow in the entry above, which teaches the decoder to read a tool name off the one event type that carries it.
- Decode the Cline and opencode event streams with the generic JSONL decoder, so a run dispatched to either shows what it is doing while it does it instead of one block of text once it finishes. Both CLIs already had a JSON mode glorp was not asking for, and both are now described rather than coded: `cline --json` wraps every event in its own envelope, so the definition reads the type from `event.type` and the text from `event.text`, and takes a tool call's name from the `content_end` whose `contentType` is `tool`; `opencode run --format json` emits one event per part of the session, so the type is the top-level `type`, the text is `part.text`, and a finished call carries both its name at `part.tool` and the input it was made with at `part.state.input`. Cline prints every assistant message three times over -- token by token as `content_start`, whole as `content_end`, and once more in the `done` event that ends the turn -- and repeats a running tool's name in a `content_update` for every chunk of output that tool streams, so the ignore list keeps only `content_end`, leaving one line per message and one `Running:` line per call rather than one per token or per streamed chunk, and opencode's `step_start`/`step_finish` bookkeeping is dropped the same way. Neither needed a line of Go: the only change outside the two definitions is that the tool-input fields a progress line is summarised from now include `filePath` beside `file_path`, since that list is read for every agent's tool calls rather than only Claude's. Both modes are asked for on a fresh run and a resume but not on the one-shot vision read, whose answer is parsed as prose, exactly as Claude's definition already does. Muse needed one thing the decoder did not have -- its text events stream token-sized fragments rather than whole messages -- and follows in the entry below.
- Link the Meta Muse Code documentation from every place glorp lists the agents it ships, so `muse` is no longer the one built-in a reader cannot click through to install. The multi-agent documentation named all six agents but linked only five, because no canonical URL for the Muse Code CLI could be confirmed when it was written; [dev.meta.ai/docs/muse-code](https://dev.meta.ai/docs/muse-code) is that page, and it carries the one-line installer and the `muse --version` check a prerequisite list is read for. It is now linked from the README prerequisites and from the built-in agents table on the agent definitions reference. The docs site's own prerequisite list linked none of the six, so all six are linked there rather than singling `muse` out, matching what the README already did. A new test derives the check from the agent registry: every built-in name must be a link in all three places, so the next definition that ships cannot land as bare text beside the rest.
- Let an agent definition say it has no resume rather than duplicating its run template to satisfy the requirement. `args.resume` is now optional when `session.assign` is `"none"` -- the setting an agent with no resumable session already declares -- and a resume for such an agent renders its `run` template instead, so a recovery re-runs the work with the recovery prompt exactly as before. `cline` and `opencode` previously copied their `run` template verbatim under `resume`, a load-bearing convention nothing stated and nothing checked, and now simply omit it. Agents glorp holds a session ID for (`codex`, `claude`, `gemini`, `muse`) still require and use their own resume template.
- Let an agent definition declare the minimum version of its CLI it works with, and check it before dispatching, so an install that is too old says so instead of failing on an argument it has never heard of. The built-in `gemini` definition was written against `@google/gemini-cli@0.58.0` and renders `--session-id`, `--resume`, and `--output-format`, none of which exist on an older release such as `0.2.2`: a dispatch there died on an unrecognized-argument error from the child process, with nothing in it pointing at the CLI's version as the cause. A definition may now carry `"minVersion"` beside `"binary"` -- `gemini` declares `0.58.0` -- and glorp asks the executable it resolved for the agent (so `--agent-binary` is what gets checked) for its version before starting the run, failing with an error naming the agent, the version found, the version required, and both ways to fix it. The version is read out of whatever banner the CLI prints around it and compared component by component, so `0.9.0` is correctly older than `0.58.0` rather than newer the way a string comparison has it, and a prerelease suffix is ignored rather than treated as unreadable. A binary whose version cannot be read at all -- because it prints none, or because asking failed -- warns in the run's output and dispatches anyway, since refusing to run a CLI that prints its version some way glorp has not seen would break more than it protects. A definition declaring no minimum, which is every other built-in, is not checked at all and starts no extra process.
- Let an agent definition declare that it accepts no reasoning level at all, so `--agent gemini:high` is rejected at startup instead of being accepted and then thrown away. An absent `levels` list meant "accept anything", which is the right default for a dimension a definition simply does not enumerate, but it left no way to say the opposite: Gemini CLI has no reasoning-effort flag, so `gemini.json` listed no levels, so every level parsed, passed validation, and then rendered into nothing, because the argv template has no `{level}` fragment to substitute it into -- indistinguishable, at the `--agent` prompt, from a level that was honoured. The `levels` and `models` allow-lists now spell three states apart rather than two: the field left out accepts any value, a list accepts exactly the values in it, and an empty list `[]` accepts none, which is what `gemini` declares. A level given to an agent that takes none is answered with `agent gemini takes no reasoning level`, in the same shape as the existing `agent level must be low, medium, or high`, and built from the definition's own name rather than from a new agent-name comparison in Go. `--agent gemini` and `--agent gemini/<model>` are unaffected, and Gemini's model list stays deliberately open because its model names move faster than a release; `codex`, `claude`, `cline`, and `muse` keep the levels they declare, and an agent that enumerates no models still takes any. The distinction survives the marshal-and-merge round trip a `.glorp.config.json` override puts a definition through, so a config file can declare an empty list for an agent of its own, while `null` keeps its documented meaning of clearing an inherited list back to accepting anything.
- Document multi-agent support, so the agent-definition schema is something you can read rather than something you have to infer from `agents/builtin/*.json`. The registry work landed six shipped agents and a config file that registers any other compatible CLI, and the only description of the schema was a single README table cell that had grown to a paragraph nobody could scan: the `--agent` row still said `codex` and `claude` were what glorp ships, `--yolo` enumerated exactly two bypass flags as though no other agent had one, and the prerequisites named two CLIs to install. The README now names all six built-ins with their install links, spells out the levels each accepts, describes `--yolo` as the per-agent `"when": "yolo"` fragment it actually is, marks `--codex-binary` and `--claude-binary` as the legacy aliases of `--agent-binary`, and says what happens on resume in the two cases that restart the work instead: an agent the current configuration no longer dispatches to, whose persisted session is discarded and redispatched, and an agent that declares no resumable session at all, whose recovery restarts it against the branch and draft pull request `gh-fix` already left open. A new reference page on the docs site carries the rest: what `.glorp.config.json` is and why it is deliberately not `.glorp.json` (glorp rewrites the state file on every dispatch, so a definition kept there is destroyed by the next save, and glorp reports the mix-up by name in both directions), both shapes the `agents` section takes, the field-by-field override and `null`-drops-it merge rules, every field of every block -- `args` fragments with their conditions and placeholders, `session`, `output` and the generic `jsonl` decoder's paths, `missingSession`, `quota`, `skills`, `env`, `levels`, and `models` -- with types, defaults, and which are required, the six shipped definitions verbatim as worked examples, a nine-step tutorial that registers an unsupported CLI from scratch without editing Go, and troubleshooting for a malformed definition, an agent missing from `glorp agents`, a resume that restarted, skills that are not installed, and an empty quota reading. A new test reflects over the definition structs and fails when a field exists in code and not in the reference, so the next schema change cannot ship undocumented.
- Decode an agent's output, capture its session ID, and recognise its "that session is gone" message from the agent's own definition, so a CLI that streams something other than Codex's plain text or Claude's event envelope is read by describing it rather than by editing `runOnce`. Reading an agent was the last part of talking to one that was still hardcoded: stdout was wrapped in Claude's `stream-json` decoder when the agent was named `claude`, scraped for a session ID with a Codex-shaped regular expression when it was named `codex`, and scanned for one global list of missing-session phrases whichever agent produced it. A definition's `output` block now selects the decoder -- `plain` for output shown as it is written, `claude-stream-json` for the existing decoder, and a new `jsonl` decoder for any line-delimited JSON stream, configured with field paths rather than Go code: which key holds the event type, which holds the text, which hold a tool call's name and its input, and which event types to skip, with a `[]` suffix stepping into an array so `message.content[].text` reads every content block. The generic decoder renders tool calls as the same `Running: ...` progress lines Claude's does, pairs a call's name with its own input rather than the next block's, passes a line that is not JSON through untouched -- banners, warnings, and stack traces are exactly what someone debugging a failed run came for -- and renders nothing at all for an event shape the definition does not describe, so an unknown or malformed event never drops the stream. Session-ID capture reads the pattern from the definition alone, with Codex's expression now living only in `codex.json`, and `missingSession` is a per-agent list added to the shared phrases rather than replacing them, so an agent whose wording is distinctive names it for itself alone -- Gemini's two messages move out of the global list onto `gemini.json`, where they no longer make an ordinary failure of every other agent read as an expired session. `GLORP_CHECKOUT_DIRECTORY=` parsing stays agent-independent, since that marker is glorp's own protocol rather than any agent's. Codex and Claude render byte-identical output through the definitions they already ship, pinned by their unchanged decoder tests, and the contract harness gains an agent that exists only as JSON -- its own event stream, its own session regex, its own missing-session phrase -- proving the whole path without a line of Go describing it.
- Make the per-agent surfaces that still assumed a fixed `codex`/`claude` pair read the agent registry instead, so an agent declared in `.glorp.config.json` is a first-class one everywhere rather than only where it is dispatched. `--agent-binary NAME=PATH` points any registered agent at a different install, repeatable once per agent, and is the only lever that reaches a config-defined agent at all; `--claude-binary` and `--codex-binary` keep working as aliases for it, and the executable it resolves to is used for the agent's quota call as well as its runs. Which quota reader an agent gets now comes from its definition's `quota` block rather than from its name: `none` by default -- reported as untracked, and costing no process on any poll -- `codex`, `claude`, or a generic `command` reader that runs an argv, reads the JSON it prints, and renders configured percent-used and reset-time fields into a template, under a timeout, keeping the last good reading when a read fails. Codex and Claude report exactly the strings they did. The settings API and the browser dashboard's agent selector list every registered agent with the models and levels each accepts, and reject an unknown one with the same list of known agents the CLI prints, so a config-defined agent is selectable at runtime without a code change. The terminal status bar truncates rather than wraps, so a run configured with four agents keeps its quota line on one row instead of pushing the job grid up the screen.
- Add a built-in `opencode` agent definition, so `--agent opencode` dispatches issues to [opencode](https://opencode.ai) without a config file and without a single new agent-name comparison in Go. It is a JSON document beside the `codex` and `claude` ones: `opencode run` with the prompt positional, `--model provider/model` for the model, `--variant` for the reasoning level (opencode's name for provider-specific reasoning effort, with `low`, `medium`, and `high` accepted so a bad value is rejected at the `--agent` prompt), and `--file` for the screenshot a browser-mode vision call reads, which opencode hands to its own read tool and attaches as an image. `--auto` is passed unconditionally rather than only under `--yolo`, for the same reason Claude gets `--permission-mode auto` when the run is not in yolo mode: `opencode run` cannot prompt, and its non-interactive default is to auto-*reject* anything it would have asked about -- which includes reaching the isolated clone the `gh-fix` skill works in, since that lives outside the directory the agent was started in, so without it the job dies on a permission nobody is there to grant. The definition declares no resumable session, because opencode exposes none glorp can use: `opencode run` prints no session ID in its default output format, `--session` accepts only an ID opencode itself minted, and `--continue` resumes whatever session in the directory happened to be last, which under glorp's concurrency is as likely to be another issue's. A recovery therefore re-runs `opencode run` with the recovery prompt instead of failing the job, which the `gh-fix` workflow is built for -- it is re-entrant and adopts the draft pull request already open. Output is read as plain text, since opencode's `--format json` event stream is its own shape rather than Claude's, and it declares the `opencode` skills.sh target, so the installers offer it the `gh-fix` and `gh-discuss` skills without either script being edited.
- Ship a built-in agent definition for Meta Muse Code, so `--agent muse` dispatches issues to it alongside `codex` and `claude` without a config file. It needs no new branching in Go: the definition is an embedded JSON document like the other two, and everything the run does with it -- rendering argv, deciding who assigns the session ID, decoding output -- goes through the registry as before. The definition was written against the shipped CLI (Muse Code 1.0.1) rather than its documentation, and the CLI's own shape settles the three questions the schema asks. `muse exec` is the headless mode and takes the prompt positionally, so the prompt is passed exactly as it is to `codex exec`. Sessions are caller-assigned like Claude's -- `muse exec --session-id <uuid>` both starts and continues a session -- so glorp generates the ID, a resume is the same invocation carrying it, and no output has to be scraped for one; a `--session-id` naming a session that no longer exists starts a fresh one rather than failing, so the ID is kept across a restart instead of being dropped the way Codex's is. Reasoning effort is `--reasoning-effort`, allow-listed to the seven values the CLI accepts (`none` through `ultra`), which is a superset of the `low`/`medium`/`high` that `--agent name/model:level` is usually written with, and the model is `--model` with no allow-list, since Muse's model ids are not enumerable from the CLI. Approvals and an OS sandbox are on by default and would stop a headless run dead, so a normal run gets `--approval-mode never`, which skips approval while leaving the sandbox in place, and `--yolo` replaces it under glorp's own yolo flag; `--user-input-auto-resolve` is passed on run and resume so a request for user input is cancelled rather than left waiting. Output is decoded as plain text: `muse exec --json` does emit JSONL, but its envelope is Muse's own event schema rather than Claude's, and a decoder for it belongs with the generic one rather than being approximated by claiming a format the stream is not. The vision call browser mode makes uses `muse exec --image <path>`, which the CLI accepts and repeats. Muse reports no quota, so the definition names no quota reader and the dashboard shows none for it. The definition names `universal` as its skills.sh target, which the registry-driven installers pick up on their own: skills.sh writes that target to `~/.agents/skills`, one of the user-scope roots Muse reads, so `muse skills list` finds `gh-fix` and `gh-discuss` and a bare `/gh-fix owner/repo#N` prompt resolves without the workspace having to be trusted first.
- Dispatch issues to [Cline](https://cline.bot) with `--agent cline`, which is now one of the agent definitions glorp ships rather than something you have to declare in `.glorp.config.json` yourself. Cline's CLI is a standalone binary that needs no editor host, and its default mode is exactly what a dispatch wants -- act mode with tool auto-approval, taking the prompt as a positional argument and never opening the TUI unless `-i` asks for it -- so the definition renders `--auto-approve true`, then `--model` and `--thinking` when the `--agent cline/model:level` spec names them, then the prompt. `--thinking` takes `none`, `low`, `medium`, `high`, or `xhigh`, and the definition's allow-list is that set, so a level Cline would reject is answered by glorp with the list instead of by Cline with a usage error one dispatch later; a model is passed through untouched, since Cline's ids are provider-scoped (`anthropic/claude-fable-5.1`) and its catalog is live. What Cline has no headless equivalent of is resume: `cline --id <session>` switches the CLI into its interactive TUI even with a prompt passed positionally, and then refuses to run at all without a terminal, so there is no session for glorp to assign or to read back from the output. The definition declares that with `"session": {"assign": "none"}` and gives a resume template that simply restarts the work with the recovery prompt, so a run glorp picks back up continues from the pull request and the branch rather than from Cline's own conversation -- which is what the recovery prompt already asks for -- instead of failing the job. Its output is decoded as plain text; Cline does have a `--json` event stream, but it is its own envelope rather than the one glorp's streaming decoder reads, so that is left to the pluggable decoding work. Quota reporting has no Cline equivalent to read. The definition names `cline` as its skills.sh target, which is the id skills.sh already knows the CLI by, so the registry-driven installers install the `gh-fix` and `gh-discuss` skills for it alongside Codex and Claude Code and a dispatched run finds the slash command it is sent.
- Install the `gh-fix` and `gh-discuss` skills for every agent glorp knows about, derived from the agent registry instead of a flag list edited by hand. Both installers called `npx skills add ... --agent codex --agent claude-code`, so an agent definition added to the registry shipped with glorp able to dispatch to it and with neither of its skills installed, until someone remembered to edit two scripts in a third language. An agent definition now carries a `"skills"` block naming its skills.sh target id -- a dedicated one such as `codex` or `claude-code`, or `universal` for a CLI skills.sh has no dedicated id for -- and a new `glorp agents` command lists the registered agents, or with `-skills` the deduplicated target ids they install skills for. `install.sh` and `install.ps1` ask the binary they have just installed for that list and build their `--agent` flags from it, so adding a built-in definition needs no installer change; a binary that cannot answer stops the install after glorp itself is in place, naming the `skills add` command to run by hand, rather than quietly installing skills for the wrong set of agents. Agents declared in `.glorp.config.json` may name a target too, and install their own skills with the same command, which the README now spells out along with the new subcommand.
- Ship a built-in `gemini` agent definition, so `--agent gemini` dispatches issues to Gemini CLI without a config file. It is data rather than code: no new agent-name branch was needed in Go, which is what the definition schema was for. Sessions work the way Claude's do -- glorp generates the UUID and hands it over with `--session-id`, and resumes it with `--resume <uuid>`, which Gemini CLI accepts alongside the index and `latest` forms its help advertises -- so an interrupted run continues rather than starting over. Its headless mode refuses to run at all in a directory nobody has trusted interactively, which is every glorp run, because the work happens in a fresh clone; the definition carries `GEMINI_CLI_TRUST_WORKSPACE=true` in the child's environment rather than passing `--skip-trust`, so the trust decision is scoped to the process glorp starts. Approvals follow Codex and Claude: `--yolo` under `glorp watch --yolo`, and `--approval-mode auto_edit` otherwise, which is as far as Gemini CLI goes without approval prompts a headless run cannot answer -- it auto-approves edits but not shell commands, so an unattended `gh-fix` run wants `--yolo` there as it does elsewhere. Output is decoded as plain text: Gemini CLI does have an `--output-format stream-json`, but it emits its own `init`/`message`/`result` envelope rather than the one glorp's stream decoder reads, so that waits for the generic decoder. It reports no quota, and takes no reasoning-effort flag, so `--agent gemini` takes a model and no level. The browser board reader hands the screenshot over the way it does for Claude, by the path the vision prompt already names, since Gemini CLI has no image flag. A resume of a session that has expired now restarts the work instead of failing the job, which needed Gemini CLI's own two wordings for it (`No previous sessions found for this project.` and `Invalid session identifier`) added to the messages glorp watches for. Its `skills.target` is `gemini-cli`, so the registry-driven installer installs the `gh-fix` and `gh-discuss` skills for it, which Gemini CLI discovers under `.agents/skills` alongside its own `.gemini/skills`.
- Cache the Go module and build caches in every workflow that sets Go up, so a run stops rebuilding the standard library and every dependency before it reaches the first test. `actions/setup-go` does cache both directories, but under a single key derived from `go.sum` with no restore-keys, which fails in both directions: a dependency bump misses that key outright and builds completely cold, and while `go.sum` is unchanged the key keeps hitting exactly, so the entry is never re-saved and the compiled objects for glorp's own packages stay frozen at whatever the tree looked like the day a dependency last moved -- which for the Linux CI job means recompiling the repository four times over, for `go test -race`, `go vet`, `GOOS=darwin go vet`, and the production-tagged pair. The built-in cache is now off and a shared composite action does the work instead: it asks the toolchain for `GOMODCACHE` and `GOCACHE` rather than hardcoding paths that differ per platform, and keys on the Go sources as well as `go.sum`, falling back through any cache for the same dependencies to any cache for the same job, so an ordinary source edit starts warm rather than from nothing. Both CI jobs, the release job, and the finalize-release job use it; the last keeps its own cache namespace because it cross-compiles six `GOOS`/`GOARCH` pairs and shares almost no compiled object with a native run, while the release job deliberately shares CI's, since it compiles the same packages the same way and runs rarely enough to be cold otherwise. The Pages workflow is untouched, as it builds only the Hugo site and uses no Go.

## v1.2.34 - 2026-09-03

- Stand down for a claim another instance posted minutes ago on the first reap after startup, instead of asking past it and dispatching work that is actively running. The previous entry closed this hole on the uncontested pickup path; the contested path kept it on its first pass, and a freshly restarted instance is exactly where it bites, because with no local work records everything it sees reads as a candidate. That first reap is deliberately aggressive -- it runs before the periodic reap timer, so work nobody holds is picked up at startup rather than waiting -- but being aggressive was implemented as skipping the claim read entirely and going straight to the ownership handshake, and the handshake only reads answers posted after its own ask. An instance that announced `Starting work on this issue` five minutes earlier does not re-announce during the grace window, so nothing answered, the ask went unchallenged, and both instances ran an agent on the same ticket. Both reaps now read the ticket's standing claims first and stand down for a foreign claim newer than the staleness window, leaving the issue marked as seen so a later poll renegotiates it once that claim goes stale. What the first reap still skips is only the guards that exist to stop a periodic pass re-asking -- this instance's own standing claim and a handshake it already settled -- so a stale foreign claim and a ticket with no claim at all are reaped immediately on startup exactly as before, and a comment read that fails there negotiates rather than stranding abandoned work until the next reap, since the handshake is itself an ask a live instance can answer.
- Read the claims already standing on a ticket before picking it up as uncontested, so two instances stop working the same issue at once. Whether an issue was contested was decided from this instance's own work records and the project board alone: an issue nobody here had a record of, whose card was not at `In Progress`, read as untouched work and was dispatched immediately, with its `Starting work on this issue` claim posted afterwards. The handoff comments other instances had already posted on the ticket were never part of that decision -- they were read only on the contested path, by the reap -- so an issue one instance announced it was starting was picked up by the next instance to poll it minutes later, and both ran an agent on it and opened their own pull request, which is what happened to issue #487. A first-time pickup now reads the same claims the reap does, on the pull request continuing the work when one is open, and stands down for a claim another instance posted within the staleness window instead of dispatching; the issue stays marked as seen, so the next poll routes it through the ownership handshake, where a claim that goes stale is still reaped as abandoned. Work this instance holds the newest claim on, work whose newest foreign claim has already expired, a resumed session, and an issue whose newest comment is an `@/glorp:ID` mention addressed to this instance are all dispatched exactly as before, the check costs one comment read per issue actually about to be dispatched, and a comment read that fails picks the issue up rather than stalling the watch.
- Describe the agent CLIs glorp dispatches to as JSON definitions instead of as `codex`/`claude` branches spread through the code, so supporting another CLI is a config file rather than a build. Every place that had to know how to talk to an agent carried its own arm of the same two-way `if`: `parseAgentSpec` accepted no name but those two, `commandArgsForSession` built two hardcoded argv shapes for a fresh run and a resume, `browserVisionArgs` built a third pair for the one-shot screenshot read, `CommandRunner.binary` mapped the name to `--codex-binary` or `--claude-binary`, and `runOnce` hardcoded Claude's `CLAUDE_CODE_PRINT_BG_WAIT_CEILING_MS=0`, Codex's stdout session-ID capture, and Claude's stream-json decoder -- six sites to edit per agent, each an opportunity to get one of the six subtly wrong. An agent is now a definition: the executable, the argv template for a fresh run, a resume, and a vision call, the environment its child process gets, whether glorp assigns its session ID or reads one back from a line it prints and whether that ID survives a failed resume, the models and levels `--agent` accepts, and how its output is decoded. `codex` and `claude` are two such definitions, embedded as JSON and loaded at startup, and the arguments they render are byte-identical to what the branches produced -- pinned by a table covering every mode, flag, and model and level combination, on top of the existing tests, which pass unchanged. `--agent` validates against the registry rather than a hardcoded pair, so an unknown name is answered with the agents that do exist instead of "agent must be codex or claude", and a level or model outside an agent's own list is answered with that agent's list.
- Read agent definitions from `.glorp.config.json`, a new file `--config` points elsewhere, so a CLI glorp does not ship can be declared without waiting for a release. The file is read-only to glorp and deliberately not `.glorp.json`: that one is the work state the daemon rewrites as issues are handled, and a hand-edited definition living there would be destroyed by the next save, so definitions found in the state file and work-state records found in the config file are both reported pointing at the other file rather than half-working. Its shape is a top-level object with an `agents` section -- an array of definitions or an object keyed by name -- leaving room for later sections instead of being a bare array. A definition whose name matches a built-in overrides it field by field, keeping everything it does not mention, with a field given as `null` dropping what it inherited; an unknown name registers a new agent, which `--agent` then accepts and dispatches through the executable its own definition names. A missing file is not an error and a default run behaves exactly as before, but malformed JSON, an unknown field, or an invalid value stops the run naming the file, the agent, and the field, because a definition dropped quietly is indistinguishable, at the `--agent` prompt, from a typo in the name.
- Prove those definitions in CI against a fake agent CLI, since the real ones cannot be run there. The vendor CLIs are not installed on the runners and most need credentials, so a definition's claims about argv, environment, session capture, and output decoding would otherwise be untested until someone ran it by hand. A small program built by the test now stands in for them: it records the argv, working directory, and environment of every invocation, and on demand announces a session ID, prints a checkout-directory marker, emits a configured output stream, reports a session it no longer holds, or exits non-zero. A reusable contract harness points a definition at it and drives a fresh dispatch, a resume, and a resume whose session is gone through the real `CommandRunner`, checking the argv of each, the session ID glorp ends up holding, the decoded output, and that a dead session restarts the work rather than failing it -- with the dead ID dropped for an agent that assigns its own and kept for one glorp assigns. `codex`, `claude`, and an agent that exists only in a config file each have a contract test on it, and the agents added later get theirs by filling in the same struct. It needs no network, runs on Windows as well as Unix, and is clean under `go test -race`.
- Record that no lever makes a headless Claude run viewable from a phone, instead of leaving the question open for a third investigation. `--remote-control` was turned off by default once `remoteControlAtStartup` proved inert under `-p`, but no replacement was looked for, so the original goal — looking in on a running agent from the Claude mobile app or claude.ai/code — stayed unmet and undocumented. Each remaining candidate was measured against Claude Code 2.1.248 with a real headless run rather than read off documentation, and all of them fail. `autoUploadSessions`, the hoped-for view-only mirror, is not a Claude Code setting at all: it is absent from the settings reference, and a `-p` run with every debug category enabled makes no upload or session-share request of any kind, so there is nothing to turn on. `claude remote-control`, the separate server mode, hosts only the sessions it creates itself, so glorp cannot hand it a `claude -p` child it already started: its `--session-id` takes a server-side code session rather than a local session UUID and rejects one as `invalid session ID: must be a cse_… or session_… tagged ID`, it refuses to start until the workspace trust dialog has been accepted interactively — which a `-p` run never triggers, because print mode skips that dialog — and it then asks `Enable Remote Control? (y/n)` on the terminal. The remaining answer is a Claude Code change that reads the setting on the print-mode startup path, filed upstream as [anthropics/claude-code#91906](https://github.com/anthropics/claude-code/issues/91906) and recorded next to the arguments glorp still passes so it is not re-derived. `--remote-control` keeps its behaviour and its default, and now says at startup, in the terminal and the dashboard log, that it currently reaches nobody and where that is tracked, rather than being an opt-in that quietly does nothing.
- Stop turning Remote Control on for Claude runs by default, because Claude does not act on it when it is launched headlessly. The default was shipped on the assumption that `--settings {"remoteControlAtStartup":true}` starts the same Remote Control bridge outside the interactive startup path, and the runtime effect was never exercised. It does not: measured against Claude Code 2.1.248, a `claude -p` run started with exactly the arguments glorp passes records no bridge session, keeps the session name Claude derives from the working directory instead of the `glorp owner/repo#N` name given to `--rc`, and is discoverable only as a session local to the machine, so nothing ever appears on a phone or at claude.ai/code. The same `--settings` payload does change the model in the same run, so the setting arrives and is simply not read under `-p`, matching what `claude --help` says of the flag it mirrors: `--remote-control` starts "an interactive session with Remote Control enabled". `--remote-control` is now off by default and kept as an opt-in that passes the same arguments as before, so a Claude release that honours the setting in print mode needs no change here, and glorp no longer advertises a capability its runs do not get.
- Say once, at startup, that `--remote-control` passes nothing to a Codex run, instead of accepting the flag on a Codex watch and silently doing nothing with it. The previous entry kept the flag as a Claude opt-in; on the Codex side there was never anything to pass, so `--agent codex --remote-control` took the flag, changed no argument, and gave no hint why the run stayed off the phone. A watch that has Remote Control on and a Codex agent configured now prints, and logs to the dashboard, that remote control is not passed to Codex runs and that they stay viewable in glorp's own dashboard; a Claude-only watch and a watch left at the default print nothing, and Codex runs themselves are dispatched exactly as before. The `codex remote-control` daemon was investigated as the mechanism that could have changed this and rejected: it is the app-server hosting the threads it starts itself, so there is nothing to hand it a `codex exec` process glorp has already started, and being hosted by it at all would mean driving Codex over the app-server protocol instead of `codex exec`. Its `start`, `stop`, and `pair` commands also refuse to run anywhere but Unix, pairing a device to it is a manual step glorp cannot complete, and starting or stopping a machine-wide daemon underneath a user who paired one themselves would disrupt their own sessions. Codex's own `remote_control` feature flag is marked removed and `codex exec` exposes no equivalent argument, so Codex runs stay local until Codex ships a per-run option. The `--remote-control` README row records all of this instead of stating only that Codex is unaffected.

## v1.2.33 - 2026-09-03

- Run ngrok through `npx` when it is not installed, so the default webhook mode no longer requires a separate ngrok install. glorp shelled out to an `ngrok` executable and failed outright when `PATH` had none, which made a manual ngrok install a prerequisite even though Node.js and `npx` already were one for the installer. `glorp watch` now looks for the configured executable first and, finding none, starts the agent as `npx --yes ngrok` instead, printing why it did so. An installed ngrok is still preferred and runs exactly as before, and a `--ngrok-binary` set to anything other than the default is still required to exist rather than being quietly replaced by a download. The first npx run has to fetch the package and the agent before ngrok says anything, so the startup budget for that path is two minutes rather than ten seconds, and npm's own deprecation and progress notices are kept out of glorp's UI while a genuine `npm error` is still reported as the reason no tunnel came up.
- Make agent runs viewable from the Claude mobile app and claude.ai/code by default, so a long run can be followed, and picked up, away from the machine it started on. glorp launches Claude headlessly with `-p`, and a headless run previously existed only inside glorp's own dashboard: there was no way to look in on it from a phone. Passing Claude's `--remote-control` flag does not help on its own, because Claude reads that flag only on its interactive startup path and silently ignores it under `-p`. Claude runs now receive `--settings {"remoteControlAtStartup":true}` instead, which starts the same Remote Control bridge without going through the interactive path and is layered on top of your own settings rather than replacing them, together with `--rc "glorp owner/repo#N"` so several concurrent runs are told apart by the issue each one is working on instead of all appearing under the same host name; a resumed run reconnects under that same name rather than adding a second entry. The new `--remote-control` flag is on by default and turns the behaviour off with `--remote-control=false`. Codex runs are unchanged, because Codex exposes Remote Control only through its separate `codex remote-control` daemon and has no equivalent argument for `codex exec`.

## v1.2.32 - 2026-09-03

- Stop a `gh-fix` run short of merging when the issue itself withholds that authorization, instead of merging anyway. An issue whose body, comments, or labels said the fix should not be merged -- a `donotmerge` or `hold` label, or a comment asking that it be left for someone to look at first -- was read for its request and nothing else, so the skill drove the pull request to green CI and merged it exactly as it would any other ticket, and the only way to keep a change out of `main` was to not dispatch the issue at all. Validation now reads the issue for a merge hold before any code is written, matching a hold label case-insensitively and ignoring separators (`donotmerge`, `do-not-merge`, `no-merge`, `hold`, `needs-review`) as well as wording anywhere in the thread that asks for the merge to stop, with the most recent comment winning so a hold can be imposed or lifted mid-run. A held run is otherwise unchanged -- it implements, tests, writes the changelog note, pushes, marks the pull request ready for review, and drives CI to completion -- and then leaves the pull request open rather than merging it, keeps the branch and the originating issue, logs which label or quoted instruction withheld the merge, and posts one comment on the pull request saying the ticket is **Ready** with the fix, tests, and CI result. A held run also files no follow-up issues, since unmerged work may still change under review; the items that would have been filed are listed in that same comment under `Follow-ups (not filed)`, linking equivalent open issues, for whoever merges to pick up.

## v1.2.31 - 2026-09-02

- Compile and run the production build in CI, instead of leaving the code a released binary actually ships untested until release day. `go test ./...` and `go vet ./...` run untagged there, so everything behind `//go:build production` -- the `webui/dist` embed, the SPA file server that backs every client-side dashboard route, the release build's no-op `StartFrontend`, and the `TestReleaseAssetsServeEmbeddedFrontend` covering them -- was never compiled, let alone run: the only build that touched that path was the release workflow itself, where a wrong embed path or a dashboard that serves nothing is far more expensive to find. The Linux job now runs `go vet -tags production ./...` and `go test -tags production ./...` after the frontend `pnpm run build` that produces `webui/dist`, and checks that directory is there first so a missing frontend build fails saying so rather than as an opaque `pattern dist/*: no matching files found` from the embed.

## v1.2.30 - 2026-09-02

- Confirm the issue is actually closed before an agent's run counts as finished, instead of taking the agent's own exit for the work being done. An agent that timed out, stopped early, or reported success without ever merging returned exactly as one that merged its pull request did, so the run was recorded as completed, the job showed a checkmark in the UI, the project card was moved to `Done`, and the ticket it left open was never picked up again -- the watch had already marked it handled. A run that ends without an error now asks GitHub for the issue's state before it is completed: closed, and nothing changes; still open, and the work is kept alive instead of finished -- a keepalive line says so in the viewport and the same session is resumed with what is still missing (check the issue and its pull request, finish the CI, review, and merge, and do not stop until it is closed), or the agent is restarted from scratch when its session cannot be reopened. GitHub is re-read once after the closure interval before a run is treated as unfinished, so a merge it has not finished processing is not mistaken for abandoned work, and a check that fails to answer at all completes the run as before rather than trapping it in a loop.

## v1.2.29 - 2026-09-02

- Fail a GitHub read that has stopped responding instead of letting it stop the watch for good. Every read `glorp watch` makes -- a `gh` invocation, a page load or script evaluation in a browser-mode tab -- ran on the run's own context with no deadline of its own, and the poll that makes them is the same goroutine that dispatches work. A page that never finished loading, a renderer that stopped answering, or a `gh` subprocess stuck on a connection therefore parked the poll loop permanently: the agents already in flight ran to completion normally, the task counters drained to nothing, and the run then sat there alive and silent, picking up no further issues and reporting no reason, because a poll that dispatches nothing logs nothing. Each browser command and each `gh` call now carries its own two-minute deadline and fails with a line naming what timed out, so the poll reports the failure and retries on its next tick rather than never returning. A tab whose command stopped returning is also dropped and reopened on the page it was showing, since every later read of that target queued behind the command that was never coming back, and closing a tab no longer waits for a command in flight, so shutdown is not held up by one either. As a backstop for anything else that could hold the loop, a run that completes no poll for four intervals now says so once and says when polling resumes.
- Deliver the issue and comment webhooks an organization-owned Projects target's runs need, instead of leaving them to the next poll. Such a target subscribed to `projects_v2_item` and nothing else, which says only that a card moved: since the previous entry, an `issues` (edited or closed) or `issue_comment` (created or edited) delivery for a ticket an agent is already working nudges that run to check GitHub immediately rather than waiting out its tick, and a board target received none of those, so a closure, an edited description, or a new comment reached the agent a whole interval late -- a gap of minutes on a board polled slowly. The board's own `projects_v2_item` hook is untouched, and each repository currently backing the board now also carries a hook subscribed to `issues`, `issue_comment`, and `ping` and nothing more: a board target dispatches from the board, so it has no use for the pushes and pull-request activity a repository target subscribes to. Those events are a subset of the repository target's, so a repository watched both ways shares one hook and keeps its wider subscription either way round. A board whose repositories cannot be listed at that moment keeps its board hook and picks the repositories up on the next reconciliation cycle, rather than losing the subscription the whole target depends on. Watching a board therefore needs webhook access on the repositories it draws from, the same access a user-owned board has always needed.
- Relay a change made to an issue while an agent is working it into that agent's own session, instead of letting it go unheard until the run ends. A run was watched only for the one thing that stopped it -- the issue or its pull request being closed -- and that stop was terminal: the agent was killed, the work was recorded as failed, and it never learned the ticket had been closed, so the branch, the draft pull request, and the isolated clone it had opened were left behind for someone to find. An edited description and a new comment were not watched for at all, so an agent that had already read the issue kept building what the issue used to say and answered nothing anyone added to the thread; the only way through was to wait out the run and dispatch a fresh one, which threw away everything the session already knew. All three now interrupt the current prompt and resume the same session with what changed: a closure asks the agent to stop implementing and clean up after itself (close its pull request, drop the branch and the clone, and report what it removed), an edited description asks it to reread the issue and reconcile the work in progress with it, and new comments ask it to reread the conversation and treat them as part of the request. The work stays active across the interruption rather than being marked failed, and a run with no session that can be reopened keeps the old behaviour of stopping on a closure rather than being interrupted for a change it could not be told about. glorp's own handoff claims (issue #214) are never relayed, so the instances watching a ticket do not restart each other's agents on their own bookkeeping, and the cleanup run a closure starts is not interrupted again by that same closure. All three watch modes are covered: poll mode and browser mode read the change on the watcher's own tick -- browser mode reads it at all now, since its issue source reported no per-issue work state and the watcher had silently been watching nothing there -- and push mode additionally uses the `issues` and `issue_comment` deliveries it already subscribes to as a nudge to check straight away, with the change itself still read back from GitHub rather than trusted from the delivery.

## v1.2.28 - 2026-09-01

- Draw from every watched Discussions target in turn as well, instead of letting one board's backlog take every slot. The issue path was balanced in the previous entry, but discussion dispatch still walked the configured targets in order and blocked on a concurrency slot inside that loop, so `glorp watch repoA/discussions repoB/discussions` with 20 unanswered threads on repoA and 3 on repoB spent every slot on repoA -- and since discussions are dispatched ahead of issues on each poll, that backlog also delayed every issue the same poll found. A poll now collects its discussion candidates across all targets before any of them waits for a slot and interleaves them with the same rotation the issue path uses, led by whichever target has the fewest discussions already running. The two paths count their in-flight work separately, so a busy issue backlog does not lead the discussion rotation or the reverse, and which threads are eligible and the concurrency limit itself are unchanged.
- Draw from every watched target in turn when dispatching, instead of queueing one target's whole backlog ahead of another's. A poll listed the targets in the order they were configured, appended each one's issues to a single list, and the dispatch loop blocked on a concurrency slot in that same order, so `glorp watch repoA repoB` with 20 open issues in repoA and 3 in repoB spent every slot on repoA and never reached repoB until repoA drained -- and since each poll re-derives the same order, the quiet repository could be starved for the life of the run. The candidates a poll produces are now interleaved across their targets before dispatch, each target keeping its own order, and the rotation is led by whichever target has the least work already in flight, so a target that filled slots on an earlier poll yields the next ones to a target that has none. Which issues are eligible, the ownership handshake, and the concurrency limit itself are unchanged; only the order they are dispatched in is.
- Split a `gh-fix` issue that clearly describes several separable pieces of work into sub-issues, and stop treating the parent as something to implement. An issue that asks for two independently deliverable things -- "implement the fix and update the docs website" -- was picked up as one ticket and produced one pull request carrying both, so the parts could not be reviewed, merged, or reverted separately and a single agent slot held the whole thing. `gh-fix` now files each part as a real sub-issue of the original through GitHub's sub-issue API and routes it exactly as it routes a follow-up (its own kind label, the parent's still-accurate labels, milestone, assignees, project `Todo` status, and the `.glorp.json` assignment), so the next agent slot can pick one up. The split is deliberately conservative: it happens only when the issue's own words name parts that could each be implemented, tested, and merged alone, never when one part is unreachable without the other ("implement the feature and fix the related lookup bug" stays one ticket) and never on the agent's own inference, because separability usually is not determinable at the start of an issue. A parent that has been split is a tracking issue: it needs no pull request, code, or changelog entry to close, and `/gh-fix` on it only decides whether it is closeable -- closing it with a comment when every sub-issue is done, and otherwise ending the run silently rather than re-splitting it or leaving another comment, so a repeatedly dispatched tracking issue neither duplicates its sub-issues nor accumulates identical comments.

## v1.2.27 - 2026-08-31

- Close the browser-mode tabs glorp has stopped reading, instead of holding every page it ever opened until the run ends. A tab is opened per watched target and one more is shared by every conversation read, and none of them was ever closed: the issue page the handoff handshake last read stayed open long after that issue was merged, so a `-no-headless` run accumulated pages nothing was looking at and a headless one kept their renderers alive for the life of the watch. A tab that has gone unread for three poll intervals (at least a minute, so a fast poll does not churn tabs between reads) is now closed and reopened only if something reads that target again. Every watched repository and board is re-read on every tick, so what actually ages out is the conversation of an issue no agent is working any more. The browser's last remaining tab is always kept, because Chrome exits when its final page target closes and the sign-in saved on shutdown is read off a tab of the still-running browser, and a reopened tab is sent to the page its predecessor was showing so a reader that polls by reloading an unchanged URL does not reload a blank tab. The browser `glorp auth` signs in through sets no timeout and keeps its tab.
- Keep `is:issue state:open` out of `--filter`'s default and append it to every query instead. The default filter was advertised as `is:issue state:open assignee:@me author:@me`, so anyone narrowing the selection had to repeat both qualifiers verbatim -- a `--filter label:ready` that forgot them fell back on glorp's own defaults only by accident of them being defaults, and a filter that named a kind or state of its own silently opted the run into work glorp cannot do anything with: a closed issue has nothing left to fix and a pull request is not a ticket it can pick up. The default is now just `assignee:@me author:@me`, and `is:issue state:open` opens every issues-page search, every browser-mode page URL, and (in the board's own `is:issue is:open` vocabulary) every project-board item query, whatever `--filter` says. A filter naming its own kind or state has that term dropped rather than contradicting or repeating the qualifiers, so `--filter "is:pr"` no longer searches for pull requests and `--filter "is:issue state:open label:bug"` is not doubled. `--all-issues`, `@me` substitution, and the requirement that the default match both assignee and author are unchanged; the README documents the qualifiers as always added.

## v1.2.26 - 2026-08-31

- Read a project board's own Status column under `glorp watch -browser`, instead of the issue state GitHub labels the title cell with. On the markup GitHub actually serves, nothing on a board row names the field a cell holds except the CSS custom property it is sized from (`--column-Status-width`): the classes are hashed CSS-module names and there are no test ids left, so the extractor's search for an attribute mentioning "status" found only the title cell's own `aria-label`, which reads `<title>, status: open` and carries the *issue's* open/closed state rather than the board's Status field. Every item on a real board therefore came back with its own title as its status, failed the ready-state gate, and was never dispatched -- a poll logged the right number of items and then did nothing with any of them. The Status cell is now identified by the column property, an item with an empty Status cell reads as having no status rather than sending the reader guessing, and a cell holding the item's own link is never read as a status. The board (column) layout is recognised by the `data-board-column` attribute a real column names its status in, and a layout is only preferred over the other when it actually produced items, so a drawn board is no longer read as an empty table because the page ships an unrelated `<table>` of its own. Scrolling a virtualized board now advances every scroller on the page by one viewport at a time rather than jumping the single largest one to the bottom: a board layout gives each column a scroller of its own, so moving only the biggest left every other column stuck on the cards that happened to fit, and jumping to the end moved the window past the items in between, which were then never in the DOM to be read at all.
- Choose the labels on a `gh-fix` follow-up issue from the follow-up itself instead of copying the originating issue's labels wholesale. Carrying the whole set across mislabels the kind of work: a follow-up filed from a `bug` arrived labelled `bug` even when it asked for a new feature, and one filed from an `enhancement` arrived as an enhancement even when it recorded a defect, so every label-based view and every triage query read the follow-up as something it is not. `gh-fix` now reads the repository's own label list and applies its equivalent of the kind the follow-up actually describes -- whatever this repository calls a bug (`bug`, `bugreport`, `defect`) or a feature (`enhancement`, `feature`) -- never inventing a label the repository does not define and leaving the kind unlabelled when it defines no equivalent. A label from the originating issue is carried over only when it still holds for the follow-up: an area, a component, a release train, a priority, or a `milestone123`-style grouping survives, while the originating issue's own kind label and its triage state (`needs-triage`, `duplicate`, `wontfix`) do not, and a carried label never overrides the kind chosen for the follow-up. Milestone and assignee inheritance, the project `Todo` routing, and the `.glorp.json` assignment are unchanged.

## v1.2.25 - 2026-08-31

- Carry an issue's labels, milestone, and assignees onto the follow-up issues `gh-fix` files for it. The skill already copied project membership and, under Glorp, assigned the follow-up to the authenticated user, but everything else the original ticket was triaged with was dropped: a follow-up filed from a labelled, milestoned, assigned issue arrived bare, so it fell out of every label-based view, out of the release it belongs to, and off the plate of whoever owned the work. Each new follow-up now inherits those three fields from the issue it came from, skipping only a value the repository will not accept (a label that no longer exists, a closed or missing milestone, a user who cannot be assigned) and saying which one it skipped. An existing open issue reused as the follow-up is filled in only where it carries nothing of its own for a field, so triage someone has already done is never overwritten, and the existing project `Todo` routing and `.glorp.json` assignment are unchanged.

## v1.2.24 - 2026-08-31

- Stagger the page loads `glorp watch -browser` makes, instead of firing one per watched page all at once on every tick. A tick reloads the issues page of every repository, every project board, and the conversation of every issue an agent is working, and it issued them back to back: a run watching a handful of repositories and a few in-flight issues hit GitHub with the whole burst the moment the tick began, N requests every interval, which is exactly the traffic pattern browser mode exists to keep modest. Every page load a run makes now goes through one shared queue that spaces them at least 2.5s apart, so five watched pages load at +0s, +2.5s, +5s, +7.5s and +10s rather than together. When more pages are being watched than fit in the poll interval at that spacing, the queue spreads them evenly across the interval instead (`interval/N`), so a full pass still completes in one interval rather than falling further behind on every tick, and it logs at most once a minute when that leaves it loading pages less than a second apart, naming the spacing it has been compressed to. Demand is measured as the pages the queue has loaded recently rather than the callers waiting on it, because the readers load their pages one after another; a page glorp stops loading ages out, so a repository dropped from the run or an issue whose agent has finished does not keep the spacing tight for everything else. The browser `glorp auth` signs in through is not paced.
- Spell the poll interval the same way in the TUI status bar, the web UI status bar, and the startup log. Both status bars read the same `Interval` off the published snapshot but formatted it independently -- the TUI printed Go's duration string and the web UI divided the nanoseconds into whole seconds -- so an hourly poll read as `polling every 1h0m0s` in the terminal and `polling every 3600s` in the browser, and the same run looked different depending on which one was open. One formatter now renders it on each side, dropping zero components, so an hour is `1h` everywhere and a short interval stays as short as it reads (`20s`, not `0h0m20s`).
- Show when glorp last checked GitHub in the TUI and web UI status bars, beside how work is being picked up (`polling every 20s; checked 14:05:09`). A poll now only writes to the log when what it found changes, so a run with nothing to do prints nothing for minutes at a time and there was no way to tell a healthy quiet loop from one that had stopped polling. The time is recorded when a poll finishes and published straight away, so it advances every tick whether or not anything was dispatched; push mode shows it too, since it still reconciles on a periodic poll, and a run that has not completed its first poll shows no time rather than an empty clock.
- Read the comments the handoff handshake runs on off GitHub's own conversation page under `glorp watch -browser`, and slow the browser-mode poll from 5s to 20s. Browser mode moved the issue list and the project board onto rendered pages but left comments on `gh api`, and comments are the heaviest API user of the loop: every contested candidate is read once per reap, and every issue an agent is working is re-read on a timer for the whole run, so a run with work in flight kept spending API quota on exactly the reads browser mode exists to avoid. Those reads now go through one shared tab of the browser the run already launched, anchored on the `issuecomment-` permalink each comment carries, and they wait for the client-rendered conversation to draw the same way the issues list does. Posting a comment has no page affordance and still goes through the API, and so does any read the page could not produce -- a page that failed to load, came back signed out, rendered markup the extractor does not recognise, or carried a comment with no readable timestamp falls back to the API rather than reporting a ticket as unclaimed and letting two instances claim the same work, and that fallback is logged once per conversation instead of on every check. The 5s default was chosen when a tick only reloaded a list; a tick that also reloads conversations and waits out GitHub's render does not fit in it, so `-browser` now defaults `-interval` to 20s. An explicit `-interval` still wins.
- Decide which handoff claim is newest by comment order when two carry the same timestamp, instead of by timestamp alone. A `Releasing this issue` withdrawal is posted moments after the `Starting work on this issue` claim it withdraws, and a wall clock that resolves to roughly 15ms -- Windows's does -- stamps both with the same time, so the withdrawal lost the comparison and the released ticket kept reading as owned by an instance doing nothing with it. Claims sharing a timestamp are now ordered by their position in the ticket's comments, which GitHub returns in creation order, both when picking one instance's newest claim and when weighing the newest foreign claim against this instance's own, so the last comment posted wins in every case.
- Stop `glorp watch -browser` printing `ERROR: unhandled node event ...` while it polls. chromedp logs an error for every DOM event missing from its own node-tree bookkeeping, and browsers keep emitting ones it does not model -- `dom.topLayerElementsUpdated` fires whenever a dialog or a popover enters the top layer, which GitHub's pages do routinely -- so a normal run interleaved unactionable `ERROR:` lines with glorp's own log. Those messages are now dropped instead of being written to the terminal; everything else chromedp reports is still surfaced.
- Wait up to 15s, rather than 5s, for a project board to render under `glorp watch -browser`. GitHub draws the Projects v2 board client-side and the reader's budget was a quarter of the one the repository issues page settled on, so a board that was merely slow was reported as `the board did not render within 5s` and could burn a `-browser-vision` screenshot from the run's budget on a board that was about to appear. A board that has already drawn still costs a single read and no wait, so the longer budget is only ever spent by a board that has not rendered.
- Withdraw a won handoff claim when the dispatch it announced is skipped. The handshake posts `Starting work on this issue` as its last step, but the dispatch that claim announces happens afterwards in the poll loop and can still be skipped -- a failed project-status move drops the candidate before any work record exists -- leaving the ticket publicly owned by an instance with no running agent: other instances read the claim and stand down for work nobody is doing, and the candidate stays contested on every later poll. A dispatch that falls through after the handshake now posts a `Releasing this issue` withdrawal signed by the same instance and drops its local record of the settled handshake, so the ticket reads as unclaimed to every instance and the next reap renegotiates it instead of reusing a decision that was walked back. The withdrawal is logged with the reason the dispatch never happened, and a withdrawal that cannot be posted is logged as such rather than passing silently. A claim posted after a withdrawal stands again, so an instance that re-claims released work still owns it.
- Keep polling while a handoff handshake waits out its grace window. The ownership negotiation that reaps a contested ticket posts `Does anyone have this?` and then waits two minutes for another instance to answer, and that wait ran on the poll loop itself: one contested issue froze the entire watch for the whole window, so no repository was re-read, no new issue was discovered, and nothing else was dispatched -- a five-second `-browser` poll that started at 21:12:21 did not finish until 21:14:25. The handshake now runs in the background and the poll returns without waiting for it. Polls during that window leave the handshake alone rather than posting a second ask on every tick, and the ticket it wins is dispatched as soon as it settles, without waiting for the next tick or reap. Losing, standing down, and the existing checks that stop a settled handshake from being re-asked are unchanged.
- Add `glorp watch -browser -no-headless`, which drives browser mode's browser visibly instead of headlessly. A failing page read in `-browser` mode previously left nothing to inspect but the CDP traffic, because the browser glorp drives is always headless; the flag puts its window on screen so a poll can be watched navigating and the page GitHub actually served can be seen. Nothing else about the run changes -- the same profile, tabs, poll interval, and readers -- and the first tab attaches to the window Chrome opens by itself rather than adding an empty one beside it. It only means anything in browser mode, so passing it without `-browser` is an error rather than a silent no-op, and it is refused on a machine where no window can appear (a Linux session with neither `DISPLAY` nor `WAYLAND_DISPLAY` set) instead of launching a browser nobody can see.
- Stop glorp re-asking who owns work it has already claimed itself. The reap that runs before reclaiming a contested ticket only looked at claims signed by *other* instances, so an instance was blind to its own: a ticket that came back around without a local work record had its whole handshake re-run, posting a fresh `Does anyone have this?` and `Starting work on this issue` pair signed by the same identity on every pass and stalling each one for the two-minute grace window. The reap now reads the newest claim on the ticket whichever instance signed it, and when that claim is this instance's own and still within the staleness window it dispatches the work without commenting again. That check reads the ticket's own comments, so the reap also keeps its own record of the handshakes it has already settled on a target and reuses that decision -- dispatch, or stand down -- rather than negotiating again, which holds even when the comment read comes back saying nothing was ever claimed. A newer claim from another instance still takes the work, and a claim, or a record, of the instance's own that has gone stale still re-opens the handshake.
- Keep the browser profile signed in to GitHub between runs, instead of needing `glorp auth` before every `glorp watch -browser`. The profile directory was already persistent, but GitHub's sign-in lives in *session* cookies (`user_session` and friends), which a browser holds in memory and drops when its process exits: only the crumbs around them (`logged_in`, `_octo`, `_device_id`) carry an expiry, so every new browser process started signed out. glorp now saves the profile's cookies when it stops a browser and injects them back before the next one navigates anywhere, covering `glorp auth`, `glorp watch -browser`, and the suspend/resume handover the in-run sign-in recovery does. The expiring crumbs are carried alongside the session cookies rather than left to the browser's own database, which a browser only commits on a batch timer of roughly 30 seconds and does not flush when it is stopped: a short-lived process such as `glorp auth` used to lose them entirely. Their expiry is carried with them, so one that has since passed is dropped instead of replayed. The cookies are written inside the profile they belong to and readable only by the user, so `-browser-profile` still picks which sign-in is used, and a profile that was signed out clears its saved sign-in rather than replaying a session GitHub has already invalidated.
- Stop the poll loop repeating itself in the log. Every tick wrote a start line, a count, and a completion line whether or not anything had changed, so a five-second poll with nothing to do produced three identical lines every five seconds; a failing poll additionally logged its cause where it was raised and again where it was returned, twice per tick, for as long as it kept failing. The poll now reports what it found only when that changes, reports a failure once (with the target or step that raised it, on the single line the caller writes) and says once when the poll recovers, and the same is true of a repeating project-board probe or discussion-listing failure. An issues page whose list container renders no rows is also read as an empty list rather than as unreadable markup, so a repository with no ready issues stops being reported as an error at all.
- Open one browser window from `glorp auth`, not two. A headed Chrome puts a window on screen as soon as it starts, and glorp then asked the DevTools endpoint for a tab of its own, which opened a second window: the user was shown an empty `New Tab` window sitting behind the `github.com/login` window the sign-in is actually driven in. The first headed tab now attaches to the window the browser already opened, so the sign-in window is the only one that appears; a browser that reports no window still falls back to opening its own tab, and headless `glorp watch -browser` runs are unchanged.
- Wait for GitHub's issues page to render before reporting it unreadable in `glorp watch -browser`. The issues list is a client-rendered React app, but the extractor ran once, immediately after the tab navigated or reloaded, so a poll that landed before the list had drawn saw no rows and no empty-state marker and failed the tick with `no issue rows and no empty-list marker were found on the page` while the repository plainly had open issues. The read is now retried until the page recognises itself, and only markup the page is actually showing is allowed to end that wait: every GitHub repository page ships a hidden `Uh oh! There was an error` blankslate for each fragment it loads lazily, and a document-wide search for an empty-state marker matched one of those on a page whose list had not been drawn, so an unrendered page was read as an empty list on the first evaluation and a repository with open issues polled as having none. The wait is bounded at 15s, GitHub's own render being routinely slower than the five seconds that was allowed at first, and a page that has already drawn still costs a single evaluation, so only a page that never renders spends the budget; it is then reported as a list that did not render rather than as markup that may have changed, which is what glorp actually met.
- Ask a project board for each search qualifier once. The item query for a `projects:` target hardcoded `is:issue is:open` and then appended the whole `--filter`, which already opens with `is:issue state:open`, so the board was asked for `is:issue is:open is:issue state:open ...` and a `--filter "is:pr ..."` contradicted itself instead of selecting what was asked for. Both qualifiers are now supplied only as defaults for a filter that does not already name a kind or a state, and a state named in the issues-page vocabulary (`state:open`) is translated to the board's own (`is:open`), which a board understands. `--filter label:ready` and `--all-issues` are unchanged, and the default filter selects the same items it did before.
- Stop repeating `is:issue state:open` in the GitHub search query glorp lists issues with. The query prepended both qualifiers and then appended the whole `--filter`, which already opens with them, so the default run searched `is:issue state:open is:issue state:open assignee:@me author:@me`. The qualifiers are now supplied only as defaults for a filter that does not name its own kind or state, so a `--filter` such as `is:pr ...` or `state:closed ...` gets the search it asked for instead of a self-contradictory one. `--all-issues` and `@me` substitution are unchanged.
- Sign the browser profile back in from `glorp watch -browser` itself, instead of only reporting that it is signed out. A private repository or board is served to a signed-out profile as a 404 or a 403, which is indistinguishable from a missing one by status alone, so glorp now asks the page GitHub actually served: a failing status confirmed by the page's own signed-out markers is read as a sign-in signal, while a signed-in run that mistyped a repository is still told it was a 404. The check costs no extra request and no agent — it piggybacks on the navigation the poll already made. On detection the run logs what it saw, stops its headless browser, opens a login window on the same profile at GitHub's login page, and resumes polling once the sign-in lands; the session persists across later runs. A sign-in that was declined or timed out is not re-offered on the next tick but backed off (10 minutes, doubling to an hour, cleared by a successful sign-in), the headless browser is restarted either way so the run keeps watching, and on a machine where no window can appear (no display server) glorp refuses to open one and points at `glorp auth` instead.
- Tell the user when `glorp watch -browser` is reading GitHub with a signed-out browser profile, instead of reporting an empty issue list forever. With `@me` in the default filter a signed-out session matches nobody, so GitHub renders a genuine "No results" page: the extraction succeeds, the poll logs `browser read 0 issue(s)`, and nothing is ever dispatched. A page that comes back with no rows is now probed for GitHub's own signed-in markers, and a page served to a signed-out session fails the poll with an actionable message naming the profile directory and pointing at `glorp auth`. Repository issues pages and project boards are both covered, the probe is a failure-path check that costs a successful read nothing, a page carrying no evidence either way is never blamed on the profile, and a signed-out page is not handed to the `-browser-vision` screenshot fallback, which cannot sign a profile in.
- Add the `glorp auth` command that browser mode has been documented against but never shipped. `glorp auth` opens a visible browser window on the same profile `glorp watch -browser` reads GitHub with, waits (up to 5 minutes) for the sign-in to finish, and closes it again, so private repositories and boards stop rendering as a 404, a 403, or a login wall; the session persists in the profile across watch runs. `glorp auth -status` reports whether the profile is signed in and as whom without opening anything, exiting 0 or 1, and both honour `-browser-profile` and `-browser-binary`. On a Linux session with no display server the command explains itself instead of waiting on a window that can never appear.
- Hydrate browser-mode issues with the body and dependency/sub-issue state the rendered issues page and project board do not carry, so `glorp watch -browser` dispatches with the real issue body and blocks on dependencies and open sub-issues exactly like the API path does, for repository targets and `projects:` targets alike. A board item's `Status` column is left untouched by the fetch. The fetch is one targeted REST read plus the existing dependency lookup, made only for issues that are dispatch candidates this instance does not already have in flight or completed, and memoized for the life of the run: a poll whose issue list did not change makes no API calls at all, and no GraphQL query is ever issued.
- Add an opt-in `glorp watch -browser -browser-vision` safety net for the day GitHub's markup changes: when the issues page loads but its rows cannot be read, glorp hands one screenshot to the configured agent and asks for the issue numbers so the run keeps dispatching. It is off by default and never runs on a schedule, on a successful read, on an empty list, or on a page that failed to load. The budget is enforced in code — one screenshot per target per 10 minutes and three per run, after which the fallback turns itself off for the rest of the run — every call is logged with its reason and running count, and an answer that is not a bare JSON list of issue numbers is discarded rather than retried. A project board that loads but never renders is now reported as the same distinguishable extraction failure an unreadable issues page is, so both are told apart from a navigation or HTTP error, and the fallback covers it too: because a board spans repositories, the agent is asked for `OWNER/REPO#NUMBER` references there rather than bare numbers, and an answer that omits the repository is discarded. Each recovered board item also carries the `Status` column read off the same screenshot, so a recovered board dispatches under `--ready-state` like a board glorp read normally; an item the board shows no status for stays undispatchable and is logged as such, with its count and reason, rather than going quiet. The per-run cap is one budget shared by issue pages and boards.
- Read the issue list from GitHub's own issues page when `glorp watch -browser` is used, instead of calling the GitHub API. Browser mode keeps one tab per watched repository, reloads it on each poll, and reads the rendered rows out of the page in a single evaluation, so polling spends no API quota, no agent tokens, and issues no GraphQL query. `-filter` and `-all-issues` keep their meaning, a list with no results is reported as no issues rather than an error, and a page that loads but cannot be read is reported once with its URL instead of on every poll.
- Read project boards through their Projects v2 page under `glorp watch -browser`, instead of the `projects(v2)` GraphQL API. A `projects:` target's items, repositories, titles, and `Status` column now come from the rendered board, so the `--ready-state` gate and the push-mode board probe work in browser mode without a GraphQL call. User, organization, and repository-scoped boards are all supported; draft cards and pull requests on the board are ignored. Moving an issue's status still goes through the API.
- Add an opt-in `glorp watch -browser` mode that drives GitHub through a headless Chrome instead of the GitHub API, with `-browser-profile` and `-browser-binary` to choose the profile directory and executable. Browser mode implies `-poll` (no webhook server and no ngrok tunnel are started), defaults `-interval` to 5s unless one is given, and fails with an actionable error when no Chromium-based browser can be launched. Without `-browser` nothing changes and no browser is started.

## v1.2.23 - 2026-08-30

- Require `author:@me` in addition to `assignee:@me` in the default `--filter`. Marking a repository issue ready for an agent now means opening it yourself *and* assigning it to yourself, so another user cannot trigger a run by assigning you an issue they filed.
- Fix `gh-fix` potentially resuming a CLOSED (unmerged) pull request when its branch name matched the `fix/issue-N-*` convention for the issue being fixed. The skill now only resumes an OPEN pull request, regardless of which signal matched it.

## v1.2.22 - 2026-08-30

- Mark repository issues ready for an agent by assigning them to yourself instead of labelling them. The default `--filter` is now `is:issue state:open assignee:@me`, glorp no longer creates or manages the `agent-ready` label on watched repositories, and the `gh-fix` skill assigns follow-up issues to the authenticated user rather than labelling them.

## v1.2.21 - 2026-08-29

- Answer the `Does anyone have this?` handoff ask when it is posted on an issue's pull request. Once a draft PR exists the handshake happens on the PR, but the running instance only recognised asks that named the issue number, so it stayed silent while actively committing and another instance took its work. Handoff ownership lookups also now resolve project-scoped work back to its repository.
- Read the ngrok tunnel URL from the log of the ngrok process glorp starts instead of the fixed local ngrok API on port 4040. When another ngrok agent already owns that port (commonly an orphaned tunnel from an earlier run), glorp used to adopt that agent's tunnel and point the GitHub webhook at a dead port while reporting the tunnel as ready. ngrok's own error output is now shown and included in the failure message when no tunnel comes up. `--ngrok-api` is deprecated and ignored.
- Fix `install.sh` silently skipping its final steps when piped into `bash`: the `npx skills add` calls consumed the piped script from stdin, so the installed-version message was echoed back as raw script text instead of running.
- Stop leftover ngrok agents from earlier glorp runs before starting a tunnel. An agent abandoned by a build that predates glorp's orphan guards (or whose reaper was killed with it) could keep running for weeks, holding the local ngrok API port and one of the account's simultaneous agent sessions, which made later runs fail with `ERR_NGROK_108`. Agents still owned by a running glorp are left alone.
- Stop resuming an issue with the agent recorded in `.glorp.json` when the current `-agent` configuration no longer includes it; the issue is dispatched to a configured agent with a fresh session instead.

## v1.2.20 - 2026-08-29

- Log the failing agent's actual output (not just the exit status) when a dispatched run fails, and include that output in the bug report link glorp generates, instead of a `[robot output omitted]` placeholder.
- Ignore `.glorp.json` state entries for repositories that are no longer being watched, so stale scoped keys from previous runs do not stop polling.
- Fix the webhook daemon starting an ngrok tunnel to its listener's wildcard address (`[::]`/`0.0.0.0`), which macOS refuses to connect to; ngrok now targets `127.0.0.1` on the bound port instead.

## v1.2.19 - 2026-08-29

- Fix `glorp upgrade` still re-running the installer for release builds whose embedded version omits the leading `v` from the latest GitHub release tag.

## v1.2.18 - 2026-08-24

- Add a settings icon to the browser dashboard that opens a modal for changing concurrency, ready-state label, allowed commenters, and the dispatched agent on a running instance without a restart.
- Have `gh-fix` crop native (non-browser) screenshots and recordings to the captured window's bounds instead of the full screen, unless the app itself is full screen.

## v1.2.17 - 2026-08-23

- Show the dispatched agent, model, and effort level in each job's TUI and web dashboard viewport.

## v1.2.16 - 2026-08-23

- Show the running glorp version in the browser dashboard's masthead.
- Skip the CI workflow's build-and-test jobs for pushes that only change Markdown files.
- Only skip dispatching an issue for having sub-issues when at least one sub-issue is still open, not merged or closed.

## v1.2.15 - 2026-08-23

- Disable Claude Code's headless background-task wait ceiling for dispatched `claude` runs, so long-lived autonomous work is no longer terminated mid-task after 10 minutes.

## v1.2.14 - 2026-08-22

- Keep the browser dashboard's Retry control enabled for queued jobs as well as active, failed, and completed work.
- Skip dispatching issues that contain sub-issues.

## v1.2.13 - 2026-08-21

- Have `gh-fix` attach a follow-up that addresses an unresolved dependency as a sub-issue of its related blocking issue.

## v1.2.12 - 2026-08-21

- Fix the `gh-fix` documentation guide's screenshot-proof image on the GitHub Pages site.

## v1.2.11 - 2026-08-21

- Replace the `gh-fix` guide's CSS-only screenshot-proof example with a representative dashboard image, so the proof section demonstrates the artifact it describes.
- Correct the GitHub CLI installation commands in the public skill guides to use `gh skill install`.
- Allow browser-dashboard jobs to be retried after completion or while active; active retries stop the current agent run before starting a fresh `gh-fix` pass.
- Retry a direct `@/glorp:ID` mention when GitHub's issue listing temporarily omits the mentioned issue, so its fresh threaded `gh-fix` run is not lost.
- Make the Pages skill-installation instructions agent-agnostic.
- Improve the public `gh-fix` guide with a stable two-line hero, a draft-PR checkpoint walkthrough, and an example of attached screenshot proof.
- Add linked GitHub Pages guides for the `gh-fix` and `gh-discuss` skills, with their installation commands, workflow diagrams, and GitHub-style visual walkthroughs.

## v1.2.10 - 2026-08-21

- Add Retry and Stop controls to each browser-dashboard job viewport, keeping unavailable actions visible but disabled, logging both intents, rerunning failed `gh-fix` work immediately, and cancelling active work on request.
- Fix the default `--filter` for repository targets no longer restricting dispatch to issues carrying the `agent-ready` label, so `glorp watch` on a repository once again only picks up labeled issues instead of every open issue authored by the current user.
- `glorp upgrade` now checks the latest published release before downloading anything. If the running binary already matches it, upgrade noops and prints the current version instead of re-running the installer.

## v1.2.9 - 2026-08-20

- Have the `gh-fix` skill prefer squash merges when the repository allows more than one merge method, falling back to a normal merge and then rebase, and use the PR's own title and body as the squash commit message instead of overriding it.
- Let issue comments directly address a running Glorp instance with `@/glorp:ID`, triggering a fresh threaded `gh-fix` pass that receives the instance identity and treats matching mentions as agent instructions. Only the *last* comment on the issue can trigger this, and only when posted by an allowed commenter — the authenticated `gh auth status` user by default, or the logins listed with the new `--allowed-commenters` flag.
- Immediately sweep issues referenced by a closed issue or pull request, resuming this instance's failed session or using the cooperative handoff before continuing work owned elsewhere.
- Have the `gh-fix` skill create actionable follow-up issues before stopping failed or stalled runs, associate them with the originating work, and route them to the matching project `Todo` status or the `agent-ready` label for Glorp-managed repositories.

## v1.2.8 - 2026-08-20

- `install.sh` and `install.ps1` now print the version that was installed instead of just the install path.

## v1.2.7 - 2026-08-20

- Fix `gh-fix` still losing track of its target repository even after v1.2.6: the repo was appended as a free-text `Repository: OWNER/REPO` line after the command, which a bare `/gh-fix N` dispatch could omit entirely (e.g. before an issue's checkout directory exists, the agent now runs outside glorp's own working directory per v1.2.5, so there was no git remote to fall back on either). `glorp` now passes the repository as part of the command itself, `/gh-fix OWNER/REPO#N`, so it can no longer be dropped.

## v1.2.6 - 2026-08-20

- Fix the `gh-fix` skill sometimes asking the user which repository to work in even though glorp always tells it, via a `Repository: OWNER/REPO` line in the dispatched prompt. The skill now uses that line directly instead of trying to infer the repository from `git remote -v`, which can fail or point elsewhere before the isolated clone exists.
- Have the `gh-fix` skill post a comment on the issue whenever it gives up before opening a pull request for any reason — not just when it stops during initial validation — so the reason is always visible on GitHub instead of only in the final report to the caller.

## v1.2.5 - 2026-08-19

- Document how `gh-fix` should upload UI screenshots and screen recordings to pull requests, using GitHub's undocumented `uploads.github.com` attachment endpoint with a bearer token instead of giving up for lack of a public API.
- Fix the coding agent sometimes targeting the wrong repository when `glorp watch` is watching more than one repo. Before an issue's checkout directory exists, the agent process now starts outside glorp's own working directory instead of inheriting it, so it can no longer mistake glorp's launch directory's git remote for the repository it was actually asked to work on.

## v1.2.4 - 2026-08-19

- Stop burning through GitHub's (much smaller) GraphQL rate limit for ordinary issue polling. The main issue-list poll and per-issue dependency lookups now use the REST Search and Issues APIs (`gh api search/issues`, `gh api repos/…/issues/…`) instead of `gh issue list`/`gh issue view`, both of which query GraphQL under the hood despite looking like plain REST commands. GraphQL is now only used for GitHub Projects (v2) boards and Discussions boards, which GitHub exposes exclusively through GraphQL. Also slow the per-issue closure/competing-claim polling loops from every 10 seconds to every 30, matching the default base poll interval rather than tripling its request rate, since every active issue runs two of these loops in parallel and each poll costs several API requests.
- Have the `gh-fix` skill post a comment on the issue explaining why no fix was implemented when it stops during validation without opening a pull request, instead of only reporting the reason to the caller.
- Fix `glorp upgrade` failing on Windows with `Copy-Item : The process cannot access the file '...\glorp.exe' because it is being used by another process`. `install.ps1` now renames the running `glorp.exe` to `glorp.exe.bak` before copying the new binary into place, since Windows allows renaming an in-use file even though it won't allow overwriting one.
- Take glorp's subprocesses down with it when glorp is killed outright. Owned subprocesses now carry a Linux parent-death signal, belong to a Windows job object that is destroyed with glorp, and on macOS and the BSDs — which have no kernel equivalent — are recorded with a small reaper process that terminates them when its pipe to glorp closes, so `kill -9` of a running `glorp watch` no longer strands the ngrok tunnel or a running agent on any supported platform.

## v1.2.3 - 2026-08-17

- Mark the `gh-fix` skill's initial `Start work on issue #N` commit with `[skip ci]`, and teach the push-triggered `CI` and `Deploy Pages` workflows to honor that marker, so opening a draft pull request no longer runs a full build against a tree identical to the default branch.
- Stop leaving subprocesses behind when glorp exits. Every process glorp starts — the ngrok tunnel, agent runs, `gh` calls, quota probes, and the web UI dev server — now runs in its own process group and is tracked, so shutting down terminates it along with anything it spawned instead of orphaning an ngrok tunnel that keeps holding the public URL. Closing the terminal (`SIGHUP`) now shuts glorp down too, and a second `Ctrl-C` kills the remaining subprocesses immediately instead of waiting for a graceful stop.

## v1.2.2 - 2026-08-17

- Restructure the CLI around subcommands: `glorp watch`, `glorp ui`, `glorp version`, `glorp upgrade`, and `glorp help`. Watching now requires the `watch` subcommand — the old top-level form (`glorp owner/repo`, `glorp --poll …`) has been removed — while `glorp --version` and `glorp -h` still work as aliases for `glorp version` and `glorp help`.
- Add `glorp ui`, which finds running glorp dashboards on localhost and opens one in the default browser. Each `glorp watch` records the dashboard port it bound, so instances started with a distant `--web-ui-port` are found without repeating the port, and a localhost scan (from port 8765 upward, or `--port`) still finds instances that left no record. When several instances are running it shows an interactive picker; on a non-interactive terminal it opens the lowest-numbered port.
- Fix a panic on the first log line when `glorp` runs with `--ui none` or `--ui tui`. The unused browser dashboard was still registered as a UI reporter as a typed-nil pointer, so the first log message dereferenced it.
- Add a `projects:` / `discussions:` target shorthand so boards no longer need a full GitHub URL: `glorp watch projects:lsegal/glorp/3 discussions:lsegal/glorp/q-a`, or `glorp watch projects:3 discussions:q-a` inside a checkout, where the `OWNER/REPO` is taken from the `origin` remote. A discussions target can now also name a single category (`https://github.com/OWNER/REPO/discussions/categories/q-a`), and only threads in that category are watched.

## v1.2.1 - 2026-08-17

- Stop probing organization-owned project boards in push mode. Those boards already deliver every card change over their `projects_v2_item` webhook, so the 30-second board fingerprint probe was duplicate polling; it now runs only for boards GitHub gives no board-level push signal for (user-owned and repository-scoped Projects). The startup line also reports the real refresh strategy in push mode (`webhook push with a 15m0s fallback poll`) instead of the unused `-interval` value.

## v1.2.0 - 2026-08-17

- Show what a Claude agent is actually working on in the dashboard progress lines: tool activity now reads `Running: Read main.go`, `Running: Grep pattern`, or `Running: WebFetch https://…` instead of a bare `Running: Read`.
- Add support for watching a GitHub Discussions board target (`https://github.com/OWNER/REPO/discussions`). glorp dispatches the new read-only `gh-discuss` skill for each new top-level Discussion thread with no replies yet; the skill only reads the repository to answer the question and posts a single top-level reply when it can do so accurately and positively, otherwise it leaves the thread untouched. Discussions boards are watched in push mode as well as poll mode: glorp subscribes the repository webhook to GitHub's `discussion` event, widening an existing glorp webhook when the same repository is already watched as a repository or Project target.
- Log every step of the cooperative handoff protocol: when a reap pass starts and in which mode, why a candidate issue looks claimed and where it will be negotiated, when "Does anyone have this?" is asked and how long the wait is, which instance answered, whether the work was picked up or let go, and which instance claimed an issue out from under a running agent. The instance identity now appears in the startup line and in handoff messages so multiple instances can be told apart in the logs.
- Restart an issue from scratch when the agent reports that the recorded session no longer exists, instead of failing the run and printing a bug report link. Sessions expire out of the agent's local history while glorp's work state keeps referring to them, and the issue workflow is re-entrant, so it picks the existing draft pull request back up.
- Detect board-only project changes in push mode by probing a cheap project board fingerprint every 30 seconds, so dragging an issue onto a board or moving a card into the ready column dispatches promptly instead of waiting for the 15-minute fallback poll.
- Detect new project board issues in push mode for user-owned Projects, which GitHub gives no `projects_v2_item` webhook, by subscribing to repository webhooks on every repository backing the board instead of leaving the whole target to the periodic poller. Those webhooks are re-reconciled on every periodic poll, so a repository added to the board later is watched without restarting `glorp`.
- Keep running when only some of a target's push webhooks can be configured, reporting the failures instead of exiting.
- Reclaim project items stranded at `In Progress` by an instance that died mid-run: a new instance now runs the cooperative handoff handshake on them instead of skipping them forever, and only stands down when another instance answers that it still owns the work.
- Keep renegotiating ownership of an issue after standing down for another instance, instead of treating it as brand new on the next poll and dispatching it anyway.
- Reap abandoned work at least every 10 minutes instead of effectively only at startup, even in push mode where the polling fallback is 15 minutes. To keep repeated reaps from spamming "Does anyone have this?", only the first reap after startup asks unconditionally; later reaps ask only when the newest claim comment from another instance is more than 2 hours old.
- Add a `glorp upgrade` command that re-runs the published install script for the current platform.
- Stop polling GitHub for webhook deliveries that cannot change which issues are dispatchable. Push, pull request, ping, and ordinary comment deliveries, along with `issues` actions such as `edited`, `assigned`, and `locked`, no longer trigger a refresh.
- Stop the webhook follow-up refresh chain as soon as a refresh actually observes the issue the delivery named, instead of always running the full retry budget.
- Increase the push-mode fallback synchronization interval from 90 seconds to 15 minutes, since webhook deliveries already provide the real-time path and the fallback only needs to recover missed deliveries.

## v1.1.0 - 2026-08-17

- Replace the manual full-version `Release` workflow input with a `major`/`minor`/`patch`/`version` level chooser that computes the next tag from the latest existing tag, matching the release bump logic from `lsegal/aviary`.
- Remove the `agent-started` issue label; the cooperative comment-based handoff protocol is now the sole authority for whether a repository issue is claimed.
- Add a cooperative handoff protocol so multiple glorp instances sharing a repository negotiate ownership through signed `/glorp:UUID` comments ("Does anyone have this?" / "Starting work on this issue" / "Continuing work on this issue" / "I am working on this") before reaping a ticket that already looks claimed, instead of silently duplicating or abandoning work. Each instance gets a random in-memory identity, and the last instance to claim a ticket wins.
- Watch for a competing starting/continuing claim from another instance while an agent is actively working an issue, cooperatively stopping the run and removing its local checkout directory if one takes over mid-run.
- Subscribe repository webhooks to GitHub's `issue_comment` event so a `Does anyone have this?` handoff comment gets an immediate `I am working on this` reply from the owning instance instead of waiting for the next poll.
- Make the `gh-fix` skill re-entrant: `/gh-fix N` now resumes an existing open draft pull request for issue `N` (or for `N` itself when it names a PR) instead of starting over. Ownership negotiation stays entirely in `glorp` itself; the skill no longer reads or posts comments to decide whether a branch is safe to adopt.
- Replace `--model` and `--model-level` with per-agent `--agent AGENT/MODEL:LEVEL` specs so each load balanced agent can run its own model and reasoning level.

## v1.0.7 - 2026-08-16

- Support repeating `--agent` to configure multiple agents, load balanced evenly across concurrency, with each agent's quota shown by name in the dashboard.
- Show Claude's actual session/week usage in the dashboard quota display instead of "not tracked", read via the free local `/usage` slash command.
- Default the repository target to the current directory's `origin` git remote when no `TARGET` argument is given.
- Redispatch a completed project item when its status is moved back to `Todo` without requiring a daemon restart.
- Fix a previously seen project item not being redispatched when its status changes to `Todo`/`Ready` (matching was case-sensitive and restricted to `In Progress`).
- Fix the checkout directory getting stuck reporting "pending" when the agent wraps its `GLORP_CHECKOUT_DIRECTORY` progress line in markdown formatting.
- Show each agent's clone status inside its viewport output area in the browser dashboard.
- Record the contributing agent CLI and model in a gh-fix pull request footer, accumulating every agent/model that has worked the issue when a different one resumes mid-flight.
- Stream Claude agent progress into the dashboard as it happens instead of showing "waiting for output..." until the job finishes.

## v1.0.6 - 2026-08-16

- Run non-interactive Claude agents in autonomous permission mode so issue workflows can use required tools instead of exiting immediately.

## v1.0.5 - 2026-07-21

- Replace separate UI-disabling flags with `--ui web|tui|none`, retaining `--no-ui` as an alias for `--ui none`.
- Move the browser dashboard status bar beneath the title area.
- Clarify gh-fix UI attachment terminology as screenshots and screen recordings.
- Inline gh-fix UI screenshots in pull request descriptions instead of committing them as repository assets.
- Build the browser dashboard with Vite only for release artifacts, while local development uses the Vite dev server and CI validates the UI's build, lint, and tests.
- Restore readable spacing between metrics in the web dashboard status bar.
- Retry webhook-triggered refreshes when GitHub's issue index lags, so newly pushed issues are not skipped.

## v1.0.4 - 2026-07-20

- Add a localhost React browser dashboard that mirrors terminal job viewports, logs, scrolling, and status, with configurable port selection and an option to disable it.
- Use the owner type encoded in GitHub Project URLs when polling, avoiding intermittent GitHub CLI owner-detection failures.
- Create individual follow-up issues for unresolved TODOs and known issues after merging an issue-fix pull request, preserving origin links and project membership.
- Stop active agent work as failed when its originating issue or an unmerged linked pull request is closed.
- Preserve completed agents' viewport scrollback and scroll position in the dashboard.
- Open issue-fix pull requests as linked drafts immediately, push development checkpoints every five minutes, and mark them ready when implementation is complete.
- Poll every 90 seconds as a fallback in push mode to reduce GitHub CLI load.

## v1.0.3 - 2026-07-20

- Show each agent's actual isolated clone directory in the dashboard and persist it for resumed sessions.
- Resume interrupted Codex and Claude sessions with their original agent and working directory.
- Keep each dashboard viewport pinned to new output only while it is at the bottom, with per-pane mouse scrolling, draggable scrollbars, and a clickable more indicator.
- Update attached GitHub Project statuses when repository-watched issues start, finish, or reset.
- Keep watching when a failed project item's status cannot be reset.
- Require issue-fixing agents to provide screenshots or recordings for completed UI changes.
- Stream live Codex standard output as readable dashboard text.

## v1.0.2 - 2026-07-20

- Reset stale locally completed or active issues and requeue them when their remote state no longer matches.
- Keep polling repositories and project boards after their initial issue scan, including when webhooks are enabled, so inconsistent issue state can be resynchronized.
- Support push-mode refreshes for organization-owned GitHub Project boards.
- Allow project boards to configure their ready status, defaulting to case-insensitive `Todo` and `Ready` matching.
- Use an available random webhook port by default and report the actual address passed to ngrok.
- Show each agent's checkout directory and session ID in its dashboard viewport, including after completion or failure.
- Launch issue-fixing agents with the repository reported by GitHub instead of relying on the current directory.

## v1.0.1 - 2026-07-17

- Prevent `--no-ui` from crashing when normal log messages are written.

## v1.0.0 - 2026-07-17

- Add the glorp robot-patcher logo and a Hugo-powered GitHub Pages site generated directly from the README.
- Add `--yolo` to opt into launching Codex or Claude without sandbox and permission checks.
- Add `--no-ui` to disable the interactive dashboard and print normal logs in a terminal.
- Watch multiple repository or project targets in one process.
- Use synchronized GitHub webhooks over a managed ngrok tunnel by default, with polling available through `--poll`.
- Show an interactive terminal dashboard with job cards, status counts, targets, scrolling logs, and push or polling state.
- Stream Codex progress into dashboard job cards, show completed jobs with a green checkmark, and display the weekly quota remaining.
- Reload and resynchronize when `.glorp.json` changes.
- Default issue watching to open issues authored by the current user.
- Support repeated `--filter` arguments using GitHub issue search syntax.
- Prevent launched agents from waiting for or reporting additional stdin input.
- Ask launched agents to summarize changes without printing code diffs or large code blocks.
- Reliably update project-board status and keep watching when a failed issue has been removed from the board.
- Always remove isolated clone directories when the issue-fix workflow exits.
- Explain and report the `project` scope required to update project-board issue status.
- Finalize releases after tags created by GitHub Actions.
- Skip project-board issues that are already in progress or completed.
- Avoid applying the `agent-started` label to issues watched through a project board.
- Add a `--version` flag and promote changelog entries during releases.
- Use the versioned changelog section as GitHub release notes.
- Fix labeling issues discovered while watching GitHub project boards.
- Recover stuck project issues from their `In Progress` board status without relying on the `agent-started` label.
- Use the `bug:` prefix for new bug-report issue titles.
- Omit the default `agent-ready` label filter when watching a project board.
- Allow selecting the agent model and reasoning level with `--model` and `--model-level`.
- Omit robot output from autofilled bug reports to prevent private data from being disclosed.
- Respect issue dependencies and leave blocked issues for a later poll.
- Parse `label=...` filter terms as GitHub label search queries.
- Update project-board issue status as agents start and finish work.
- Report the required `read:project` scope when project-board polling cannot access project items.
- Add a scrubbed autofilled bug-report URL when an agent exits unsuccessfully.
- Preserve agent session IDs after an issue is completed.
- Remove `agent-ready` and `agent-started` labels after the originating PR is merged.
- Ensure the `agent-ready` and `agent-started` labels exist when watching a repository.
- Add issue label filtering with a default `label=agent-ready` filter and an `--all-issues` override.
- Track active agent work with the `agent-started` issue label and persisted session state.
- Allow watching GitHub repository and project URLs as targets.
